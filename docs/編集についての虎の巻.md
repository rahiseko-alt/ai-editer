# 編集についての虎の巻

このファイルは、**AI が動画の「どこを・どこで切るか」を決めるときに毎回読む判断基準** である。
プロも素人も当然のようにやっていて、わざわざ言語化されない「編集の初歩の初歩」を、AI が実行できる形に落としたもの。
迷ったときは、ここに書かれた **優先順位と決定表（§7）** に従って選ぶこと。「望ましい」ではなく「こちらを選べ」と読むこと。

---

## 0. なぜこれが要るのか（この文書が生まれた事故）

2026-08-17、AI が切り出す範囲として **「画編集の話」** という文字範囲を選んだ。
正しくは **「動画編集の話」** である。「動画編集」という 1 語の途中（「動」と「画」の間）にハサミを入れた。

人間の編集者は、素人であってもこの候補を **出すことすらできない**。なぜなら人間は文字列ではなく
「動画編集」という **意味の塊** を見ているからで、その真ん中に切れ目が存在するという発想が起きない。

AI は文字列（あるいは正規化された文字の連なり）を見ているので、この事故が起きる。
だから **「意味の塊を壊さない」ことを明示的な制約として持たせる** 必要がある。それがこの文書の存在理由である。

---

## 1. 大原則（すべての判断の上位にある 5 つ）

この 5 つは、下位のどの節の規則よりも優先する。

### 原則1: 切ってよいのは「境目」だけ。境目でない場所は切れない

言葉にも音にも「境目」がある。境目とは、そこに切れ目を入れても
**元々そこにあった切れ目と区別がつかない** 場所のこと。

- 言葉の境目 ＝ 文の終わり ＞ 読点相当の意味の切れ目 ＞ 文節の切れ目
- 音の境目 ＝ 発話と発話の間の無音（ポーズ）の内側

**単語の途中・文節の途中・音の立ち上がりの途中は「境目ではない」。ここは切れない。**
候補として出すこと自体が誤り。

### 原則2: 切り出した一片は、それ単体で意味が通らなければならない

切り抜きは前後の文脈を失う。だから一片は「単体で読んで分かる」必要がある。
判定は次の 1 問だけ：**その一片だけを人に見せて、何の話か分かるか？**

分からないなら、範囲を広げるか、別の場所を選ぶ。範囲を広げられないなら **その候補は捨てる**。

### 原則3: 迷ったら「広く取る」。狭く取って壊すより、広く取って冗長なほうが害が小さい

言葉が欠けた動画は直せない（素材から作り直しになる）。長い動画は後から詰められる。
**非対称なので、常に広い側へ倒す。**

### 原則4: 「元々そこにあったもの」を残す。合成しない・作らない

無音を「足す」のではなく、素材に元々あった間（ルームトーン＝現場の環境音を含む静けさ）を
**捨てずに残す**。完全な無音は人間が日常で経験しないため、合成した無音は不自然に聞こえる。
（出典 §3-出典）

### 原則5: 均一化しない

すべての間を同じ長さにする、すべてのフィラーを消す、すべての繋ぎ目を同じ処理にする——
これらはすべて「機械が編集した音」になる。**間の長さは、その場所が何であるかによって変える。**

---

## 2. どこで切るか（言葉の側）

### 2-1. 切ってよい位置の序列（上から優先）

| 順位 | 位置 | 説明 |
|---|---|---|
| 1 | **文の終わり** | 「〜です。」「〜ます。」「〜だ。」「〜だよね。」の直後。最も安全。 |
| 2 | **意味の切れ目（読点相当）** | 話し言葉には句読点が無いので、息継ぎと接続の切れ目で判断する。「〜なんですけど、」「〜して、」の直後。 |
| 3 | **文節の切れ目** | やむを得ないときだけ。下記 2-2 参照。 |
| — | **文節の内側／単語の内側** | **禁止。候補にしてはいけない。** |

### 2-2. 文節とは何か（切ってよい最小単位）

文節 ＝ 「発音や意味の上で不自然にならないように、文をできるだけ短く区切ったまとまり」。
判定法は **「ネ」を入れて自然かどうか**。

```
私の／兄は／今日／羽島に／行きます
（私の“ネ” 兄は“ネ” 今日“ネ” 羽島に“ネ” 行きます）
```

**間違えやすいところ（ここを間違えると単語を割る）**

- 「勉強する」は **1 文節**（割らない）。ただし「勉強を／する」は 2 文節。
- 「走っている」は **走って／いる** の 2 文節（補助動詞は分ける）。「〜してみる」「〜しておく」「〜してしまう」も同様。
- **複合語は 1 語として扱い、絶対に割らない**：「動画編集」「文字起こし」「切り抜き動画」「視聴維持率」。

> 【最重要】この規則が守られていれば「画編集」は生まれない。
> 「動画編集」は複合名詞 1 語なので、その内部に境目は存在しない。

### 2-3. 助詞・文末の扱い

- **助詞（は・が・を・に・で・と・から・けど・ので…）で始まる断片を作ってはいけない。**
  助詞は直前の語にくっついて 1 文節を作るので、助詞の前で切ると文節を割ったことになる。
- **文末表現は必ず最後まで含める。** 「〜と思うんですよ」の「よ」まで。語尾を落とすと言い切りの強さが変わる。
- **体言止め（名詞で終わる）は、話者が実際にそう言った場合のみ許す。** AI が動詞を落として体言止めを作るのは禁止。

**悪い例／良い例**

```
✕ 悪い: 「画編集の話」          ← 複合語「動画編集」を割った
◯ 良い: 「動画編集の話」

✕ 悪い: 「を全部やり直したんです」  ← 助詞「を」で始まっている
◯ 良い: 「そこを全部やり直したんです」

✕ 悪い: 「めちゃくちゃ大変だったと思うん」  ← 文末が途中で切れた
◯ 良い: 「めちゃくちゃ大変だったと思うんですよ」

✕ 悪い: 「って言われて、で、そのあと」    ← 前も後ろも宙に浮いている
◯ 良い: 「上司にやり直せって言われて、そのあと徹夜しました」
```

### 2-4. 「単体で意味が通る」とはどういう状態か

切り出した一片の **先頭** に次のものがあると、単体では意味が通らない。**先頭を後ろへずらすか、範囲を前へ広げる。**

| 先頭にあると危険なもの | 例 | 対処 |
|---|---|---|
| **接続詞** | 「で、」「それで、」「だから、」「でも、」「なので、」 | 落として次の文から始める。ただし落とすと因果が消える場合は、前の文ごと含める |
| **指示語** | 「それは」「これが」「あの件は」「そういうのは」 | **指示先を含めるまで範囲を前へ広げる**。広げられないなら候補を捨てる |
| **応答・相槌** | 「はい」「うん」「そうですね」 | 落とす（ただし §4-1 の例外を見ること） |
| **逆接の受け** | 「〜ですけど、」で始まる | 前の主張ごと含める |

一片の **末尾** に次のものがあるのも同じく禁止。

| 末尾にあると危険なもの | 例 |
|---|---|
| **言い差し（言いかけて終わる）** | 「これがですね、」「なんというか、」 |
| **接続助詞で終わる** | 「〜なんですけど、」「〜して、」「〜すると、」 |
| **主語だけ／目的語だけ** | 「その件は」 |

> **例外：言い差しは「意図的な余韻」としてなら許される。**
> ただし条件がある——(a) 話者が実際にそこで間を置いている、(b) 続きが無くても意味が閉じている
> （例：「まあ、そういうことです。」）。この 2 つを両方満たさないなら切らない。

### 2-5. 逐語抜き出し（keepText）を作るときの追加規則

このリポジトリは AI に「文字起こし本文から連続した部分文字列を抜き出させる」方式を取る。
このとき **部分文字列であることは正しさを保証しない**。以下を必ず守る。

1. 抜き出しの **先頭は文の先頭**（または少なくとも文節の先頭）に置く。
2. 抜き出しの **末尾は文の末尾**（文末表現の最後の文字）に置く。
3. 抜き出しを機械的に短縮・延長してはならない。**短縮が必要なら「文を丸ごと落とす」形で行う。**
   端を文字数で削る（前から n 文字／後ろから n 文字）のは **常に誤り**。
4. **4 文字・10 文字といった「最低文字数」で採否を決めない。** 文として閉じているかで決める。

---

## 3. どこで切るか（音の側）

### 3-1. 音の切れ目は「無音の内側」にしか無い

言葉として正しい位置を決めたあと、**実際にハサミを入れる時刻** は次の手順で決める。

1. 言葉として切りたい位置の近くで、**実測した無音区間**（音量が閾値を下回っている区間）を探す。
2. その無音区間の **内側** に切り点を置く。両端からは必ず余裕（数十ミリ秒）を空ける。
3. 無音区間が見つからない（＝話が途切れずに続いている）なら、**そこは切れない。切り位置を変える。**

**なぜ内側でなければならないか**

- 無音の **手前側の端** を越えて食い込むと、直前の語の **余韻（減衰）** を削る → 語尾がブツッと途切れる。
- 無音の **後ろ側の端** を越えて食い込むと、次の語の **立ち上がり（アタック＝語頭の子音）** を削る。
  日本語は語頭の子音が語の識別そのものなので、ここを数十ミリ秒削るだけで
  「カット」が「アット」に、「そうです」が「うです」に聞こえる。**最も壊れ方がひどい。**

### 3-2. 音声認識のタイムスタンプを、そのまま切り位置にしてはいけない

Whisper 系の単語タイムスタンプは **音響的な境界とは一致しない**。同じ音声でも実行環境で
100〜400ms 差が出ることが報告されており、研究上の評価許容も 200ms が標準。
**つまり単語境界ちょうどで切ると、平均して語頭か語尾を数百ミリ秒削る。**

→ **単語タイムスタンプは「だいたいこの辺」を知るためだけに使う。**
　 実際の切り点は、必ず **音量から実測した無音区間** で決め直す。
　 実測できないなら、前後に **のりしろ（handle）** を付けて広く取る（原則3）。

### 3-3. ブレス（息継ぎ）の扱い

**ブレスは雑音ではなく、文の一部である。**

| 状況 | どうするか |
|---|---|
| 文と文の間のブレス | **残す。** ここが最も自然な繋ぎ目になる |
| 切り出しの **先頭** に来るブレス | **残す。**「息を吸って話し始める」のが自然。いきなり声から始まると唐突に聞こえる |
| 切り出しの **末尾** のブレス | 残してよい（余韻になる）。ただし長すぎるなら短くする |
| 大きすぎて耳障りなブレス | **消さずに音量を下げる。** 消すと不自然な穴が空く |
| 繋ぎ目でブレスが足りないとき | ルームトーン（現場の環境音）で埋めて隙間を作る。**無音を貼らない** |

**切るなら「ブレスの前か後ろか」**：**ブレスの終わりで次の語を切り込む** のが定石。
つまり「ブレス＋次の語」を 1 セットとして扱い、ブレスの手前にハサミを入れる。

### 3-4. 無音（ポーズ）のどこにハサミを入れるか

- ハサミは **無音の中央付近** に入れる。端に寄せると 3-1 の事故が起きる。
- **間を「消す」のではなく「短くする」。** 2.0 秒の沈黙を 0.3 秒にするのは短縮だが、
  0 秒にするのは破壊。
- **繋ぎ目の間を全部同じ長さにしない**（原則5）。目安：
  - 同じ話題の中の繋ぎ目 → 半呼吸（短め）
  - 話題が変わる繋ぎ目 → 一呼吸（長め）
  - 素材が元々持っていた間より **長い間を作らない**（作ると「溜め」が捏造される）

### 3-5. プチッというノイズを防ぐ（ゼロクロスと極短フェード）

波形が 0 でない位置で切ると、振幅が不連続に飛んで **クリック／ポップ** が鳴る。
対策は 2 つあり、実務では両方使う。

1. **ゼロクロス点**（波形が振幅 0 の線を横切る点）に切り点を合わせる。
2. **極短のフェード／クロスフェード** を掛ける。1ms 程度から効き始め、数ミリ秒〜十数ミリ秒で
   ほぼ聞こえなくなる。人間には「フェードした」と分からない長さ。

**注意：フェードを長くすれば安全、ではない。** フェードイン区間は音量が下がっているので、
**語頭に掛かるとアタックが痩せる**。フェードは「繋ぎ目のごく短い区間」に限る。

### 3-6. J カット / L カット（音と画をずらす）

- **J カット**：次のクリップの **音が先に** 始まり、あとから画が切り替わる。
- **L カット**：前のクリップの **音が残ったまま** 画だけ先に切り替わる。
- どちらも「スプリット編集」と呼ばれ、**画と音を同じ瞬間に切らない** のが要点。
- **重ねる位置は、自然なポーズかブレスの上**。考えの途中に重ねてはいけない。

**なぜ効くか**：画と音が同時に切り替わると、切れ目が二重に主張して「切った感」が出る。
ずらすと切れ目が 1 つに感じられ、会話が途切れずに繋がって聞こえる。

**このリポジトリでの位置づけ**：現状の実装は画も音も常に同じ瞬間に切っている（§8-K）。
1 人の話者を切り抜く用途では致命的ではないが、複数区間を繋ぐ digest では
**繋ぎ目が全部「同時カット」になり、繋いだ感が出る**。

---

## 4. 何を捨てるか

### 4-1. フィラー（えー、あー、あの、その、なんか、まあ）

**全部消すのが正解ではない。** 理由は 3 つある。

1. **全部消すと「台本を読んでいる」ように聞こえる。** 実際の話者はフィラーを自然に使う。
2. **フィラーは次の思考と密接に位置している。** 消すとリズムが壊れる。
3. **同じ表記でも役割が違う。**

**役割の見分け（最重要）**

| 表記 | 言い淀み（消せる） | 指示語・副詞（消してはいけない） |
|---|---|---|
| あの | 「**あのー**、そうですね」 | 「**あの**資料を見てください」 |
| その | 「**そのー**、なんていうか」 | 「**その**話は前にしました」 |
| まあ | 「**まあ**、えっと」 | 「**まあ**いいでしょう」（譲歩の意味を持つ） |
| なんか | 「**なんか**、あの」 | 「**なんか**変な音がする」（＝何か） |

**判断規則：**
- **固定の単語リストで消してはいけない。** 前後の文脈で役割を判定する。
- **迷ったら消さない。** 消し損ねた「えー」より、消えた「あの資料」のほうが害が大きい。
- 消す割合の目安：1 つの塊の中でフィラーが半分を超えることは実際には無い。
  半分を超える判定が出たら、その判定自体が壊れている。

### 4-2. 言い直し・重複・言い淀み

日本語の文字起こし業界には処理段階の呼び名がある。**どこまでやるかを最初に決めること。**

| 段階 | 内容 |
|---|---|
| **素起こし（逐語録）** | 一言一句そのまま。フィラーも言い直しも全部残す |
| **ケバ取り** | 言い淀み・相槌・言い間違い・重複した語尾を落とす |
| **整文** | さらに文として通じるよう整える（語順・助詞の修正を含む） |

**このリポジトリの立場：音は素材そのものを使う（合成音声を作らない）ので、「整文」は原理的に不可能。
できるのは「ケバ取り相当のカット」までである。** 整文が必要な結論を出したら、その候補は捨てる。

**言い直しの処理：**
```
素材: 「これはですね、あ、違う、これがですね、一番大事なんです」
✕ 悪い: 「これはですね、これがですね、一番大事なんです」  ← 訂正だけ消して重複が残った
◯ 良い: 「これがですね、一番大事なんです」                ← 言い直し前ごと落とす
```
規則：**言い直しは「訂正前＋訂正の合図（あ、違う、じゃなくて）」をひとまとまりで落とし、訂正後だけ残す。**

### 4-3. 間を詰めすぎると何が壊れるか

| 壊れるもの | 症状 |
|---|---|
| **呼吸** | 息継ぎの場所が無くなり、聞いている側が息苦しくなる |
| **リズム** | すべての繋ぎ目が同じ長さになり、機械が読んでいるように聞こえる |
| **感情** | 「溜め」が消える。驚き・言いにくさ・重さは全部「間」が担っている |
| **理解** | 聞き手が内容を飲み込む時間が消え、情報が流れていく |

**規則：意味の切れ目（話題の変わり目・結論の直前・強調の直後）の間は詰めない。**
詰めてよいのは「やり取りが進んでいない時間」（物を探している・紙をめくっている・機材を触っている）だけ。

**詰めてはいけない沈黙の具体例：**
- 相手の反応を待っている間
- 質問したあとの返事待ち
- 考えている間
- 言いにくいことを言う直前の溜め

---

## 5. 切り抜き（ショート動画）特有の作法

### 5-1. 冒頭の掴み

- 視聴者は **2〜3 秒** で見続けるか決める。3 秒以内に離脱する層が多数を占める。
- 冒頭 0〜3 秒で **1 つの状況を示し、1 つの約束をする**。
- **最初のカットは、動画全体で最も強いフレーズを置く。** 挨拶・自己紹介・前置きは冒頭に置かない。
- **時系列を守る必要はない。** 山場を冒頭に持ってきてよい。
- 字幕は **1 語目から** 出す。

**悪い例／良い例**
```
✕ 悪い: 「えーと、今日はですね、動画編集の話をしようと思うんですけど」
◯ 良い: 「動画編集で一番やっちゃいけないのが、これなんですよ」
```

### 5-2. 単体で成立させるための文脈の与え方

切り抜きは前後を失う。足りない文脈は次の順で補う。**捏造はしない。**

1. **範囲を広げて、素材の中の説明部分を含める**（最優先）
2. **hook / テロップで補足する**（音は足せないが文字は足せる）
3. 1 も 2 もできないなら **その候補を捨てる**

**捨てるべき候補の見分け**：指示語の指す先が素材の別の遠い場所にある、
専門用語の説明が別の場所にある、笑いの前提が別の場所にある。

### 5-3. 尺が変われば構成そのものが変わる

**尺は「文字数の上限」ではなく「構成の指示」である。**
同じ素材から 60 秒と 90 秒を作って、内容がほぼ同じになったら **考えていない証拠**。

| 尺 | 構成 |
|---|---|
| 〜45 秒 | 主張 1 つだけ。前置き・導入・具体例は全部捨てる。最も強い一文から始めて結論で終える |
| 〜90 秒 | 主張 1 つ＋裏づけの具体例 1 つ。45 秒版に具体例を「足す」のではなく、具体例が入る前提で主張の選び方から変える |
| 〜180 秒 | 掴み→具体例 2 つ以上→山場→締め。起伏を組む。短い版を引き伸ばさない |
| 180 秒〜 | 小テーマ 2〜3 に分ける。テーマごとに山場を 1 つ。最後に全体を締める |

**規則：尺を変えたら、選ぶ区間の「集合」が変わらなければならない。端を伸縮させるだけの調整は禁止。**

---

## 6. アンチパターン（機械的に切ると起きる不自然さ）

| # | やってはいけないこと | 何が起きるか |
|---|---|---|
| A1 | 文字数・秒数で機械的に端を削る | 単語の途中で切れる（「画編集」） |
| A2 | 音声認識の単語境界ちょうどで切る | 語頭のアタック／語尾の余韻が数百 ms 欠ける |
| A3 | 無音を全部・一律の長さに詰める | ジャンプカット感。呼吸が消え、感情が消える |
| A4 | フィラーを単語リストで全部消す | 「あの資料」の指示語まで消える。台本読みに聞こえる |
| A5 | 繋ぎ目を全部同じ間にする | 機械が編集した音になる |
| A6 | 前後に余裕（のりしろ）を取らずに切る | 直せない。素材から作り直しになる |
| A7 | 画と音を常に同じ瞬間に切る | 切れ目が二重に主張して「繋いだ感」が出る |
| A8 | 指示語・接続詞で始まる断片を出す | 単体で意味が通らない |
| A9 | 部分一致で拾えたところを採用する | 別の場面の発言が 1 本に繋がる／語の途中から始まる |
| A10 | 尺の指定に対して端だけ伸縮する | 60 秒版と 90 秒版が同じ内容になる |
| A11 | 「間」を作るために無音を合成して貼る | 環境音が途切れて「音が死んだ」区間ができる |
| A12 | フェードを長くして安全側に倒す | 語頭が痩せる。フェードは短くする方が安全 |

---

## 7. 迷ったときの決定表（この順で機械的に判定する）

```
Q1. 切り位置は単語（複合語含む）の内側か？
    YES → 切れない。文節境界まで動かす。動かせないなら候補を捨てる。
    NO  → Q2 へ

Q2. 切り出した一片は、単体で読んで何の話か分かるか？
    NO  → 範囲を広げる。広げられないなら候補を捨てる。
    YES → Q3 へ

Q3. 先頭が接続詞・助詞・指示語か？ 末尾が接続助詞・言い差しか？
    YES → 先頭を後ろへ／末尾を先へ、文の境界まで動かす。
    NO  → Q4 へ

Q4. その時刻に、実測された無音区間があるか？
    NO  → その時刻では切れない。最寄りの無音まで動かす。無いなら候補を捨てる。
    YES → Q5 へ

Q5. 切り点は無音区間の内側（両端から余裕あり）か？
    NO  → 内側へ寄せる。
    YES → Q6 へ

Q6. 消そうとしている間は「やり取りが進んでいない時間」か？
    NO（相手待ち・考え中・溜め） → 消さない。
    YES → 短くする（0 にはしない）。

Q7. 消そうとしている語は、文脈上ほんとうに言い淀みか？
    判断がつかない → 消さない。
```

**最後の砦：どの Q でも判断がつかないなら、「広く残す」を選ぶ。**

---

## 8. 現状の実装が、この虎の巻のどの原則に違反しているか

該当ファイルを読んで確認した違反。上から重い順。

### 【致命】A. 部分一致が単語の途中から切り出す — 「画編集」を生む直接の実装

`/home/user/ai-editer/video-shorts/src/reverse-match.mjs:146-157`

```js
function longestSuffixMatch(joined, target, fromPos = 0) {
  for (let len = target.length; len >= 4; len--) {
    const p = joined.indexOf(target.slice(target.length - len), fromPos);
```

`target.slice(target.length - len)` は **文字列を後ろから 1 文字ずつ削って** 一致を探す。
`target` は正規化済み（記号・空白を除去した文字の連なり）なので、**単語境界の情報が完全に失われている**。
「動画編集の話」に対して len を 1 減らすと **「画編集の話」** が生成される。これがそのまま採用される。

同じ問題が `longestPrefixMatch`（`:134-144`、`target.slice(0, len)` で末尾を 1 文字ずつ削る）にもある。

- 違反：**原則1（境目でない場所で切っている）／§2-2 複合語を割らない／§2-5-3（端を文字数で削るのは常に誤り）**
- 下限が `len >= 4`（`:136`, `:149`）＝**4 文字の断片でも採用される**。§2-5-4 違反。
- 直す方向：正規化の段階で **文字位置 → 単語 index の対応（`charToWord`）を持っているのだから、
  縮める単位を「文字」ではなく「単語」にする**。`buildCharIndex`（`:25-36`）が既にその対応を持っている。

### 【致命】B. AI へのプロンプトに「どこで切るか」の指示が 1 行も無い

`/home/user/ai-editer/video-shorts/src/select-segments.mjs:10-27`（`SYSTEM_RULES_BASE` / `OUTPUT_SCHEMA`）
`/home/user/ai-editer/video-shorts/src/digest-editor.mjs:98-100`（`VERBATIM`）, `:192-201`（`draftPrompt`）

現在 AI に伝えているのは「一字一句そのまま連続で抜き出す」「要約・改変・創作は禁止」だけ。
**「文の先頭から始める」「文末まで含める」「複合語を割らない」「指示語・接続詞で始めない」が一切無い。**
`VERBATIM`（`digest-editor.mjs:98`）は「実在する連続した部分文字列」と定義しており、
**文字列としての部分列であればよい＝単語の途中から始めてよい、と明示的に許してしまっている。**

- 違反：**§2 全体（言葉の側の切り方）／§2-4（単体で意味が通る）**
- 直す方向：`SYSTEM_RULES_BASE` と `VERBATIM` に §2-1〜§2-4 の規則を入れる。
  特に **「keepText の先頭は文の先頭、末尾は文末表現の最後」** を必須条件にする。

### 【致命】C. 採否を「文字数 4 以上」で決めている

`/home/user/ai-editer/video-shorts/src/select-segments.mjs:95`
```js
.filter((s) => s && typeof s.keepText === "string" && s.keepText.trim().length >= 4)
```
`/home/user/ai-editer/video-shorts/src/digest-editor.mjs:306`, `:379`, `:396` にも同じ `>= 4`。

4 文字の断片（「画編集の」）が通る。**文として閉じているかを一度も見ていない。**

- 違反：**§2-5-4／原則2**
- 直す方向：文末表現で終わっているか・先頭が接続詞／助詞でないかの検査を追加する。

### 【重】D. 切り点を単語タイムスタンプで決めている（音の実測をしていない）

`/home/user/ai-editer/video-shorts/src/reverse-match.mjs:60-61`（`start: startWord.start` / `end: endWord.end`）
`/home/user/ai-editer/video-shorts/src/snap-boundaries.mjs:204-238`（`snapStart` / `snapEnd` も word の start/end へ寄せるだけ）

Whisper の単語タイムスタンプは音響境界と 100〜400ms ずれる（§3-2）。
そのまま切り点にすると **語頭のアタック／語尾の余韻を削る**。

`breath-handles.mjs` は `silencedetect` で実測しているが、これは **digest 経路の「間を戻す」処理だけ**
（`planBreathParts`）。topic 経路の `snapToSilence` は **音を一度も見ていない**（word 間ギャップという
「文字起こし上の隙間」を無音とみなしているだけ）。

- 違反：**§3-1／§3-2／原則3（のりしろが無い）**
- 直す方向：`snapToSilence` にも `detectSilences()` の実測を渡す。少なくとも **前後 100ms 以上ののりしろ** を付ける。

### 【重】E. `snapToSilence` が「最大ギャップ」を選び、意味の切れ目を見ていない

`/home/user/ai-editer/video-shorts/src/snap-boundaries.mjs:181-238`

- 探索窓 ±1.5 秒の中で **最も長いギャップ** の word 境界へ寄せる。
  文の終わりかどうか・文節の切れ目かどうかを **一切見ていない**。
  長い「溜め」（§4-3 で詰めてはいけないと決めた沈黙）が最大ギャップになるので、
  **意図的な溜めの真ん中を切り点に選びやすい。**
- 寄せた結果 **`keepText` が更新されない**（`:196-201` は `start`/`end`/`duration` だけ差し替え）。
  → 台本テキストと実際の音がずれ、字幕・承認画面が実物と食い違う。

- 違反：**§2-1（序列を無視）／§4-3（溜めを切る）**

### 【重】F. `capSegmentDuration` が「等分点」で機械的に割る

`/home/user/ai-editer/video-shorts/src/snap-boundaries.mjs:82-102`、特に `:95`
```js
const end = isLast ? seg.end : (nearestWordEnd(idealEnd, words, cursor, seg.end) ?? idealEnd);
```

- 分割位置は **理想の等分点** から探す。文の切れ目・話題の切れ目を見ていない。
- word が疎なとき **`?? idealEnd` で理想点をそのまま採用** ＝ 発話の真っ最中で切る。
  コメント（`:76`）自身が「語の途中で切れる可能性はある」と認めている。

- 違反：**原則1／§2-1／A1**
- 直す方向：等分点の近くにある「文末」を探す。見つからないなら **割らずに諦める**（長いまま出す）。

### 【重】G. `mergeShortSegments` が、捨てたはずの発話を復活させる

`/home/user/ai-editer/video-shorts/src/snap-boundaries.mjs:132-154`

```js
const start = head.start;
const end = tail.end;            // ← 間のギャップも丸ごと含まれる
const keepText = group.map((g) => g.keepText || "").join(" ");  // ← テキストは連結しただけ
```

`maxGapSec`（既定 3 秒）以下のギャップを跨いで結合するが、区間は `head.start`〜`tail.end` なので
**AI が「要らない」と判断して除外した最大 3 秒ぶんの発話が、そのまま動画に戻る。**
しかも `keepText` にはその発話が含まれないので、**台本と音が不一致になる**（承認したものと違う動画が出る）。

- 違反：**原則2（一片が単体で意味が通る保証が壊れる）／§2-5**

### 【中】H. 固定フィラー一覧が既定で有効

`/home/user/ai-editer/video-shorts/src/trim-plan.mjs:20-27`
```js
export const FILLERS = [ ..., "あのー", "あの", "そのー", "その", "まあ", "まー", ..., "なんか" ];
```
`:154` — `judge` が渡されないと `isFiller(w.w)` にフォールバックする。

`trim-judge.mjs` の AI 判定は **後から足された経路** であり、`planTrim` は `opts.judge` 省略を
「正当な後方互換の選択」として許している（`trim-plan.mjs:106-113` のコメント）。
つまり **judge を通さない呼び出しでは、いまも「あの資料」の「あの」が消える。**

- 違反：**§4-1（固定リストで消してはいけない／同じ表記でも役割が違う）**
- 直す方向：固定リストは **候補抽出専用** と明記し、`judge` 無しで `cutFillers` を有効にできないようにする。

### 【中】I. 詰めた後の間が全部ちょうど 0.3 秒になる

`/home/user/ai-editer/video-shorts/src/trim-plan.mjs:39`（`AFTER_SPEECH_MARGIN_SEC = 0.3`）と `:185-195`

コメント自身が「詰めたあとの継ぎ目はどれも必ずちょうど 0.3 秒の間になる」と書いている。
**これは原則5（均一化しない）の正面からの違反。** 話題の変わり目も、同一話題の中も、
結論の直前も、全部 0.3 秒になる。

また判定が **二値（詰める／詰めない）** しかないので、`judge` が「詰めてよい」と答えた
2.0 秒の間は **一律 0.3 秒に潰れる**。§3-4 の「短くする、0 にしない」は満たすが、
「どれくらい短くするか」を誰も決めていない。

- 違反：**原則5／§3-4**
- 直す方向：間の長さを「その場所が何か」（話題の変わり目／同一話題内／溜め）で変える。
  `breath-handles.mjs:191-199` は既にこの考え方（gap を上限にする・話題の変わり目は一呼吸）を実装しているので、
  **trim 側もそれに揃える**。

### 【中】J. `trim-judge` が AI に「語の羅列」しか見せていない

`/home/user/ai-editer/video-shorts/src/trim-judge.mjs:60-94`（`buildTrimPrompt`）, `:255-269`

- 見せるのは `添字<TAB>語` の 120 語ぶんのリスト（`:61`）。**句読点も文の区切りも無い。**
  §4-1 の「文脈で役割を判定する」ができる形になっていない。
- チャンクは `i += size` で **重なり無しに分割**（`:255`）。しかも文の途中で切れる。
  「あの／資料／を」の「あの」がチャンク末尾に来ると、次の語が見えないまま判定される。
- チャンクを跨ぐ間は候補に出さない（`:257-259` のコメント）ので、**そこは永久に詰まらない**。
  結果、間の長さが「チャンク境界かどうか」というユーザーに無関係な理由でばらつく。

- 違反：**§4-1（文脈で判定）／原則5（理由のない不均一）**

### 【中】K. 画と音を常に同じ瞬間に切っている（J/L カットが無い）

`/home/user/ai-editer/video-shorts/src/trim-plan.mjs:436-463`（`buildTrimFilters`）

`trim`（映像）と `atrim`（音声）に **同じ区間** を与え、`concat=n:v=1:a=1` で束ねている。
仕様上、映像と音声のカット点をずらす経路が存在しない。

- 違反：**§3-6**
- 位置づけ：1 話者の切り抜きでは致命的ではないが、digest（複数区間を繋ぐ）では
  **全繋ぎ目が同時カットになり「繋いだ感」が出る**。§3-6 の効果を得るには構造的な追加が要る。

### 【軽】L. `breath-handles` は方針としては正しいが、適用範囲が狭い

`/home/user/ai-editer/video-shorts/src/breath-handles.mjs:1-31`（方針コメント）

この実装は §3-1（無音の内側）、§3-3（合成無音を貼らない）、§3-4（一律にしない）、
原則4（元々あったものを残す）を **正しく実装している**。虎の巻に照らして最も整合的なコード。

問題は **digest 経路にしか掛かっていない** こと（topic 経路の `snapToSilence` は §8-D/E のまま）と、
`:27-31` の残課題（カット端のフェード未実装）が残っていること。

- 直す方向：**この方針を topic 経路にも展開する**。新規実装を書くより、既存の考え方を広げるほうが速い。

### 【軽】M. `digest-editor` の尺是正が「構成を変える」保証を持たない

`/home/user/ai-editer/video-shorts/src/digest-editor.mjs:260-274`（`durationFixPrompt`）, `:391-412`

プロンプト文では「端を詰めるな・区間を丸ごと落とせ」と正しく指示している（`:268-270`）が、
**採否の判定は「目標秒数へ近づいたか」だけ**（`:402`）。
端を 1 秒ずつ削っただけの修正でも近づけば採用される。§5-3 の「区間の集合が変わる」検査が無い。

- 違反：**§5-3／A10**

---

## 出典

**§2（言葉の側）**
- 文節の定義・「ネ」判定・補助動詞／複合語の扱い: https://gakonai.com/bunsetsu/
- 文節を分けるときの見分け方: https://benesse.jp/kyouiku/teikitest/chu/japanese/japanese/c00470.html
- 字幕は文節を意識して改行する／助詞で始まる行を避ける／意味の塊の途中で切らない: https://coconala.com/blogs/4308938/402252 , https://vook.vc/n/1339 , https://www.k-intl.co.jp/blog/B_211020A
- 「やさしい日本語」分かち書きルール（文節単位）: https://www.pref.shizuoka.jp/_res/projects/default_project/_page_/001/015/583/2rule_1.pdf
- 話し言葉は文の区切り・息継ぎのタイミングで切る: https://capworks.jp/blog/cut-editing/ , https://media-hakase.com/column/article/page_3207.html

**§3（音の側）**
- ブレスは自然な会話の一部。全部消さず音量を下げる／ルームトーンで隙間を埋める／クロスフェード: https://www.production-expert.com/production-expert-1/5-techniques-for-dialogue-editing-in-film-and-tv
- 自然なブレスの終わりで次の語を切り込む／ルームトーンで置き換える: https://pacific-content.com/editing-um-dialogue-28725d12ef29/ , https://www.slaterooffilms.com/blog/5-ways-to-clean-up-dialogue-track
- ゼロクロス点で切らないとクリック／ポップが鳴る: https://www.wooji-juice.com/blog/zero-crossings-and-you , https://link.springer.com/chapter/10.1007/978-3-031-40067-4_20
- 極短フェードでクリックを防ぐ／1ms から効き数ミリ秒〜十数ミリ秒でほぼ聞こえなくなる: https://www.electronicproduction.co.uk/post/audio-fades-the-key-to-clean-seamless-edits , https://www.soundonsound.com/techniques/removing-clicks-crossfades-sonar
- J カット / L カット（スプリット編集）、重ねるのは自然なポーズかブレスの上: https://www.soundstripe.com/blogs/a-video-editors-guide-to-j-cuts-and-l-cuts , https://www.epidemicsound.com/blog/j-cuts-and-l-cuts/ , https://www.studiobinder.com/blog/what-is-a-j-cut-in-film/
- Whisper の単語タイムスタンプは 100〜400ms ずれる／評価許容が 200ms: https://arxiv.org/pdf/2509.09987 , https://github.com/m-bain/whisperX/issues/1247 , https://openai-whisper.mintlify.app/guides/word-timestamps
- カット前後に余白を残すと自然になる／クロスフェードでブツ音を消す: https://trierstyle-movie.com/crossfade/

**§4（何を捨てるか）**
- フィラーを全部消すと台本読みに聞こえる／リズムが壊れる: https://pacific-content.com/editing-um-dialogue-28725d12ef29/ , https://www.getlisten2it.com/blog/how-to-delete-filler-words-without-making-your-podcast-sound-robotic/
- 全部消すと choppy になる／過剰処理: https://www.theshanman.com/blog/is-there-software-to-automatically-edit-out-ums-and-uhs-from-a-podcast
- 間は情報を飲み込む時間を与える: https://podcast.adobe.com/en/guides/from-rough-to-ready-a-beginners-guide-to-podcast-editing
- 素起こし／ケバ取り／整文の定義と使い分け: https://mojiokoshi3.com/ja/post/suokoshi-kebatori-seibun/ , https://koelab.jp/blog/blog-802/ , https://rimo.app/blogs/transcription-arrange

**§5（切り抜き特有）**
- 2〜3 秒で判断される／3 秒以内の離脱／0-3 秒で状況と約束を提示／字幕は最初の語から: https://www.truefuturemedia.com/articles/science-of-short-form-video-hooks , https://virvid.ai/blog/first-3-seconds-hook-faceless-shorts-2026 , https://www.opus.pro/blog/youtube-shorts-hook-formulas
- 切り抜きは前後の文脈が失われるのでテロップで補足する／山場を冒頭に置く: https://find-model.jp/insta-lab/youtube-clipping-video/ , https://monocromo.jp/blog/92/

**リポジトリ内の先行調査（§3-3 / §3-4 の「一呼吸・半呼吸」「合成無音を貼らない」の根拠）**
- `/home/user/ai-editer/video-shorts/src/breath-handles.mjs:21-25` に記載の出典
  （production-expert / filmpac / ruahcreativehouse / note.com）

### 未確認（調べたが一次情報を取れなかった項目・数値を断定していない箇所）

- **§3-1 の「両端から数十ミリ秒空ける」の具体値**：一次情報で数値の合意を確認できていない。
  リポジトリ内の実装値（`breath-handles.mjs:45` の `SILENCE_MARGIN_SEC = 0.02`）は
  この案件の経験値であって、業界標準として確認したものではない。
- **§3-1 の「語頭の子音を数十ミリ秒削ると別の語に聞こえる」**：日本語音声学の一次文献は未確認。
  「アタックを削ると不自然になる」ことは複数の実務記事で確認できたが、
  **何ミリ秒で何が起きるかの定量的な根拠は未確認**。
- **§3-3 の「切り出しの先頭にブレスを残すと自然」**：実務記事の記述（自然なブレスの終わりで
  次の語を切り込む）から導いたもので、「先頭にブレスを残せ」と明記した出典は未確認。
- **§3-5 のフェード長の推奨値**：「1ms から効く」「数ミリ秒〜十数ミリ秒でほぼ聞こえない」は出典あり。
  ただし **「日本語の話し声に対する最適値」は未確認**。
- **§5-3 の尺ごとの構成表**：区間数の目安（1〜2 / 2〜4 / 4〜7 / 7 以上）は
  `digest-editor.mjs:112-132` の既存実装から引いたもので、**外部の一次情報は未確認**。
- **§3-6 の J/L カットを「日本語の 1 話者切り抜きに適用したときの効果」**：
  出典はいずれも英語圏の劇映画・対話編集を前提にしている。**この用途での検証は未確認。**
