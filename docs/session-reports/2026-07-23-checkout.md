# kosespark Check-out — 2026-07-23

> branch: main / HEAD: d0206e9
> **次セッションで本ファイル先頭から Read 必須**

## 🔴 最重要

1. **字幕 ASS タイミングずれは根治済み**（`d0206e9`）。起票時の推定原因（setpts と ass の順序）は**誤り**で、実測すると出力シーク（`-i` の後の `-ss`）が filtergraph より後段で適用されるのが真因だった。修正は 2 段シークの廃止（入力シーク 1 段化）。3 区間で再現→根治を実測済み。
2. **`work/hensyu01/lecture.mp4` の用途がマスター確認待ち**（P1 #1）。07-22 に配置され文字起こしまで済んでいるが `state.json` が無く pipeline 未 init。**編集モード / 字幕有無 / 縦横のヒアリングをせずに着手するな**（自治憲法 §4 の機械強制事項）。
3. **kosespark に H2大義が無いため、新規方向性の plan は `plan-daigi-gate` で必ず deny される**（P1 #2）。今回はマスター判断で Plan Mode 手動解除により回避した。

## A. 本セッション TL;DR

1. `/pc-checkin` の実体照合で「記録と実体が 2 段ずれ」を検知。memory.md P1 と直近レポートは 07-05 で停止していたが、実体は 07-12 の 3 commit（配布フロー AI 運転型刷新）＋未コミット 2 系統＋worktree 内の未回収パッチまで進んでいた
2. `plan-daigi-gate` が product では `照合先: N/A` を拒否し ExitPlanMode を機械 deny することを実コードで確認。マスター判断で Plan Mode 手動解除により回避（大義は今回立てない）
3. 統治設定の伝播（hook 13本 + settings.json）を vibe-base 正本と diff 全件一致で照合してコミット
4. 作業痕（`scratch_frames/` / `.claude/state/`）を .gitignore へ移し、追跡済み PNG 4枚の削除を反映
5. digest 尺目標 `--target-min` を実データ検証のうえコミット（話速 4.45〜5.16 字/秒・全尺指定で誤差 0 字・不正値は従来動作へフォールバック）
6. **字幕バグを実素材で再現**（出力 t=0.5s に ASS の 2.26–2.56s 行が焼かれていた＝常に 2 秒先行）→ showinfo でフィルタ入力の `pts_time=0` を計測して真因を特定 → 入力シーク 1 段化で根治 → 3 区間（300s / 700s / 0s）で期待値一致を実測
7. worktree `agitated-wescoff-4e45ec` を削除しブランチも削除（回収すべきパッチは実測で無効と判明したため取り込まず、diff はバックアップ済み）
8. `build-dist.mjs` で配布版を再生成し src と dist の一致を確認

## B. 完了 commit 一覧

| hash | 種別 | 内容 |
|---|---|---|
| `b3b61e1` | chore | vibe-base 統治設定の伝播（hook 13本 + settings.json）※メッセージ先頭に `@` 混入（後述 F） |
| `8f19462` | chore | 作業痕を .gitignore へ（scratch_frames / .claude/state）+ PNG 4枚削除 |
| `9e98fe4` | feat | digest モードに尺目標 `--target-min` を追加 |
| `d0206e9` | fix | 字幕 ASS タイミングずれを根治（出力シーク → 入力シーク 1 段） |

## C. 影響波及

- **`renderClip` の ffmpeg 引数から `PREROLL` / 粗シーク / 精シークが消えた。** 今後 `-ss` の位置を触る変更は、必ず字幕焼き位置の実測（本レポート E の手順）とセットで行うこと。ASS は「区間先頭 = 0」の相対時刻で書かれているため、filtergraph に流れる先頭フレームの PTS が 0 = 区間先頭であることが前提条件になっている。
- `pipeline.mjs` の `select` が `--target-min <分数>` を受け付ける（digest モードのみ有効・省略時は従来どおり）。
- `video-shorts/samples/test-16x9.mp4` を合成生成した（143KB・`video-shorts/.gitignore` で追跡外）。これがあると `tests/render-check.mjs` が通る。消すと再び FAIL するので、次に FAIL したら本レポート E の ffmpeg コマンドで再生成せよ。

## D. 残作業

**H（優先度高）**
- `work/hensyu01/lecture.mp4` の用途をマスターに確認（編集モード / 字幕 / 縦横のヒアリング必須）

**M（優先度中）**
- kosespark の H2大義＋マイルストーンの起案 → マスター承認（新規方向性の plan を出すには必須）
- `digest-editor.mjs` の `--target-min` / 尺是正ループにテストが無い（検証パネル レンズ② advisory）。`tests/smoke.mjs` は reverse-match / srt-builder 側のみを覆っており、尺換算と是正分岐は未カバー

**L（優先度低）**
- render 拡大ガードの 1080p 以上実素材でのリグレッション確認（`lecture.mp4` が 2048×1046 なので実施可能になった）
- textlint プリセット 2 件が 1 メジャー遅れ（07-05 から継続・マスター保留中）

## E. 再現手順（次に字幕タイミングを触る時に使う）

検証素材の合成（`render-check` が「合成動画がありません」で落ちたら実行）:

```bash
ffmpeg -y -f lavfi -i "testsrc=size=1920x1080:rate=30:duration=6" -f lavfi -i "sine=frequency=440:duration=6" -c:v libx264 -preset veryfast -c:a aac -shortest video-shorts/samples/test-16x9.mp4
```

filtergraph に流れる先頭フレームの PTS 実測（真因特定に使った計測）:

```bash
ffmpeg -y -ss 298 -i <素材> -ss 2 -t 12 -filter_complex "[0:v]scale=1080:1920:force_original_aspect_ratio=decrease,showinfo[v]" -map "[v]" -frames:v 3 -f null -
```

字幕位置の実測は `renderClip` を直接呼んで出力からフレーム抽出し、ASS の Dialogue 定義時刻と突き合わせる。本セッションの検証スクリプトは scratchpad に置いた（セッション限りのため次回は作り直す）。

## G. 検証パネル結果（完遂宣言前・orchestration-discipline 規律3）

レンズ選定: ② 正当性（code-reviewer）＋ ③ セキュリティ（security-reviewer）。settings.json に権限差分があるためレンズ③を含めた。UI 変更が無いためレンズ⑤は非該当。

**両レンズとも VERDICT: PASS / blocking 0 件。** advisory は以下（すべて対応方針を確定済み）。

| レンズ | 指摘 | 判定・対応 |
|---|---|---|
| ② | `render-vertical.mjs` の A/V 同期コメントが旧2段シーク時代の実測値の引用に見える | **対応済**。新方式で 3 クリップ再実測した値である旨をコメントに明記した |
| ② | `p.start` が素材長を超えると ffmpeg が空 mp4 を exit 0 で返し `runFfmpeg` が成功として resolve する | **pre-existing**。旧2段シーク版でも同一挙動であることをレビュアーが手動再現済み。pipeline.mjs 側に出力検証が無い既存ギャップ。本 diff 由来ではないため今回は触らない |
| ② | `digest-editor.mjs` の `measure()` は `tr.words` 欠落 transcript で常に 0 を返し、不要な「伸ばせ」是正が 1 回走る | **通常経路では再現不可**（transcribe.py / transcribe_groq.py は words を必ず出力）。手編集 transcript のみ。実害は LLM 1 回分 |
| ② | `--target-min` / 尺是正ループのテストが `tests/` に一件も無い（render-vertical 側は render-check.mjs があるのと非対称） | **残作業へ**（D の M に記載）。plan 外のため今回は着手せず |
| ③ | `Bash(npm publish:*)` が deny と ask の両方にある | **pre-existing**。`586c002` 時点で既に重複しており本セッション由来ではない。vibe-base 正本には重複なし。deny 優先のため実害ゼロ |
| ③ | `session-start-handover-inject.mjs:194` の `@参照` に containment チェックが無くパストラバーサル可能 | **kosespark では不到達**（`isOrchestrator` の判定材料 `docs/html-toranomaki/registry.json` が無い）。ただし **vibe-base では registry.json が実在し到達する**。vibe-base は作業範囲外のため申し送りのみ |

レビュアーが反証を試みて「壊れない」と確認した項目: 尺是正ループの多重/無限化（`while` ではなく単発 `if`）、`targetMinutes` 不正値のフォールバック、`meta` の null 経路、`p.start=0` / 素材末尾近傍 / 極端に短い dur での renderClip 実行、`PREROLL` / `coarse` / `fine` の残骸参照（grep で実体参照なし）。

## F. 申し送り（不備・未解決）

- **`b3b61e1` のコミットメッセージ先頭に `@` が混入している。** Bash ツールで PowerShell の here-string 記法を使ったミス。`git commit --amend` も `git reset` も権限 Deny のため修正できず履歴に残った。実害はメッセージの見栄えのみ。
- worktree `agitated-wescoff-4e45ec` の未コミット差分は scratchpad にバックアップしたが、**scratchpad はセッション限りで消える**。内容は「hook 群の古い伝播 + 無効と判明した render-vertical.mjs パッチ」で、いずれも失って困らないと判断した。
- 07-05 P1 #2 のインスタライブ 7 本は `video-shorts/output/` が空で実体が存在しない（追跡外のため復元不可）。
- **vibe-base 側へ渡すべき指摘が 1 件ある**（作業範囲外のため kosespark からは触っていない）: `vibe-base/.claude/hooks/session-start-handover-inject.mjs:194` の `refPath = path.join(projectDir, ref)` に containment チェックが無く、`memory.md` の `@参照` に `../` を書くと projectDir 外を読んで注入できる。vibe-base は `docs/html-toranomaki/registry.json` が実在するため `isOrchestrator` が true になり**この経路に到達する**。外部入力からは到達せず（memory.md を書けるのは AI 自身かマスター）実リスクは低いが、AI が memory.md を壊した時の被害範囲が広がる。
