# kosespark Check-out 2026-07-02 — 文字起こし Groq クラウド化（コード完成・鍵有効化待ち）

> branch: master ／ 次セッションは本ファイルを先頭から Read してから着手すること。
> plan: `~/.claude/plans/twinkly-herding-lighthouse.md`（承認済）

## 🔴 最重要

> **【同日セッション後半で解決済み】** 401 の主因は .env に古い無効鍵が残っていたこと（検証で .env が環境変数より優先されるため気づけず）。別 Google アカウントで発行した gsk_（56字）を .env に入れて解消。**Groq 10倍速を実証**（48.7秒動画: Groq 5.4s vs local 53.8s・word 粒度あり・reverse-match 互換）。詳細 → `docs/session-reports/2026-07-02-transcribe-speed.md`。以下の 1 は解決前の記録。

1. **Groq API キーが 401（Invalid API Key）で通電できていない**。鍵は Active・末尾 `hu8O` 一致・形式クリーンなのに拒否＝**Groq 側の問題**（新規鍵が invalid を返す既知不具合）。鍵は `video-shorts/.env`（git 追跡外）に設定済み。鍵が Groq 側で通れば `auto` バックエンドが即 Groq を使う。
2. **コード側は完成**。`auto` は鍵無効・ネット断で自動的にローカル faster-whisper へ退避するため実害ゼロ（実機で fallback→words=160 完走を確認）。
3. **素人（マスター）が Groq 鍵取得で 3 回以上詰まった** = P0 即死仮説#1（素人が導入・操作できるか）への実データ。同時に §0「API 登録代行」がまさに代行すべき摩擦であることの裏付け。次回司令塔判断で運用メモ化を検討。

## A. 本セッション TL;DR

- マスター要望「文字起こし高速化（精度込み）＋複数本同時処理」をリサーチ（Explore 3体並列）。
- 結論: ローカル CPU は int8+VAD 適用済で天井が低く、複数本同時は原理的に不可。速度・並列は **Groq whisper-large-v3-turbo（216倍速・$0.04/時・無料枠1日8h）を束ねる**のが技術・§0 両面で正解。方針＝Groq メイン＋ローカル無料 fallback の二段構え。
- plan 作成・承認（plan-finalize: 承認後モード=default／外部API・課金・鍵のため）。
- 実装: `transcribe_groq.py` 新規 / `transcribe.py` を `--backend auto/local/groq` にディスパッチャ化 / `requirements.txt` 新規 / README 追記。
- 検証: py_compile OK・ローカル後方互換 OK（words=160・スキーマ `w/start/end` 一致）・auto フォールバック OK。
- code-reviewer 指摘2件を修正（フォールバック二重失敗の未処理例外・.env クォート除去）。指摘1件（dict(resp) デッドコード）は SDK 仕様変更耐性の防御コードとして残置。

## B. 本セッションの変更（コミット対象・パス限定）

| ファイル | 種別 | 内容 |
|---|---|---|
| `video-shorts/src/transcribe_groq.py` | 新規 | Groq バックエンド（verbose_json→既存スキーマ変換・鍵は .env/env から・値ログ出力なし） |
| `video-shorts/src/transcribe.py` | 修正 | `--backend auto/local/groq`・auto フォールバック（groq失敗→local）・二重失敗ハンドリング |
| `video-shorts/requirements.txt` | 新規 | faster-whisper / groq |
| `video-shorts/README.md` | 修正 | 文字起こしバックエンド節（比較表・鍵設定・並列の原理）追記 |

> `video-shorts/src/gen-editor-html.mjs` / `video-shorts/docs/` は別セッション成果のためコミットに含めない。

## D. 残作業（次セッション・鍵有効化が前提）

- [H] Groq 鍵の 401 解消（Groq 側）。console.groq.com で鍵 Active 確認済・伝播待ちでも 401。選択肢: 鍵削除→再作成／Groq サポート問合せ／OpenAI gpt-4o-mini-transcribe へ切替（plan リスク欄）。
- [H] 鍵有効化後: `python src/transcribe_groq.py work/talk-16x9/input.mp4 out.json` で **word-level timestamp が返るか実確認**（plan ステップ1・粒度が words か segments か）。返れば reverse-match 互換確定。
- [H] Groq 経路 end-to-end 通電（→reverse-match→render→candidates.json）＋ local vs groq 速度実測を `docs/session-reports/*transcribe-speed*.md` に記録。
- [M] 複数本同時投入（`pipeline-runner.mjs` ジョブ並列化・Groq 時のみ）。plan ステップ6。

## E. 配備済 subagent / skill / hook

- なし（本セッション追加なし）。

## F. 本セッション生成レポート

- 本ファイル（引継ぎ）。plan は `~/.claude/plans/twinkly-herding-lighthouse.md`。

## G. 次セッション初手

1. 本ファイルと plan を Read。
2. マスターに Groq 401 の現況を確認（鍵が有効化されたか／別 API に切替るか）。
3. 鍵が通れば plan ステップ1（word-level 粒度確認）→5（速度実測）→6（並列）を順に実行。
4. 機械検証サマリ（本セッション）: py_compile OK・local後方互換 OK・auto fallback OK・secret grep 0件・`.env` git 追跡なし。
5. コードレビュー指摘: 3件（2件修正済・1件は防御コードとして残置）。
