# kosespark Check-out 2026-06-16 — video-shorts 実験実装

- branch: master
- 次セッションは本ファイルを先頭から Read してから着手すること
- plan: ~/.claude/plans/woolly-questing-planet.md

## 🔴 最重要
- **video-shorts は kosespark の2つ目のテスト版エンジン**（1つ目=engine/記事生成）。長編動画→縦型ショート自動編集。即死仮説#1（素人がClaude Codeを使える）の検証台候補として実装。**ゲート判定は未実施**（実験のみ・P0 進行は別途）。
- 大容量動画（output/ work/ samples/ ui/*.mp4）は `.gitignore` で除外済。**コミットされるのはコードのみ**。再現は下記「次セッション初手」で。

## A. 本セッション TL;DR
1. video-shorts ツールを新規実装（やる事レポート7タスク準拠）。場所: `products/kosespark/video-shorts/`
2. 6段パイプライン: 文字起こし(faster-whisper) → LLM区間選定 → **逆マッチング(秒数をLLMに出させない)** → FFmpeg縦化＋字幕焼き → 選別UI
3. faster-whisper 1.2.1 をマスターが pip install（環境: ffmpeg8.1/python3.12/node24）
4. 実証1: TTS音声50秒 → end-to-end 通電（3区間）
5. 実証2: **実素材（サッカー日本代表会見 sample6.17.mp4・10分52秒）→ 6トピックの縦型ショート生成成功**（逆マッチングで重複なく時系列確定）
6. 仕様変更反映: 引き延ばし廃止→上下帯ぼかし背景 / バズ抜粋→トピック分割 / 派手字幕（特大白＋極太黒縁、見出し黄色）
7. 派手エフェクト3種を `src/apply-effect.mjs` に実装（glitch/neon/shake・全FFmpeg標準・CPU・無料・VBVキャップでサイズ暴走防止）
8. CPU無料エフェクト技術リサーチを `docs/video-effects-cpu-free-research.md` に保存（出典=外部AI生成・URL未検証を明記）

## B. 完了 commit
- 本 Check-out コミットで video-shorts/ 一式（コード16ファイル）＋ docs 2件 ＋ memory.md を追加

## D. 残作業（次セッション）
**H（堅牢化・code-reviewer指摘・現状は実害ゼロだが将来踏む）**
1. `src/render-vertical.mjs:62` `escapeFilterPath` がシングルクォート/スペース/コンマ未処理 → ASS字幕パスに特殊文字があるとfilter文字列破損（※spawn配列渡しなのでシェルインジェクションは無し）
2. `src/reverse-match.mjs` normalize が空を返すwordがあると charToWord/words のインデックスずれ → endWord が別wordを指す境界バグ
3. `src/apply-effect.mjs` `-vf`ブランチ(glitch/shake)に `-map 0:a?` 無し → 音声マッピングがffmpegバージョン依存（現状は音声入っている）
4. `src/transcribe.py:97` output がファイル名のみ（親dir無し）だと `os.makedirs("")` で FileNotFoundError

**M（エフェクト・機能拡張・マスター判断待ち）**
5. エフェクト採用判断: glitch/neon/shake のどれを6本全部に一括適用するか（マスター「初版良い」で保留）
6. glitch は noise のぶん他より重い（41MB）。採用なら強度調整
7. xfade トランジション（リサーチ①・即足せる）/ 吹き出し字幕（Remotion別建て・重い）は未着手

**L**
8. `src/select-segments.mjs:107` モデル名 `claude-opus-4-8` ハードコード

## E. 配備済 subagent/skill/hook
- なし（既存のみ使用）

## G. 次セッション初手
1. 本レポート → `memory.md` P1 → plan(`~/.claude/plans/woolly-questing-planet.md`)を読む
2. video-shorts 再現: `cd products/kosespark/video-shorts` → `node pipeline.mjs init <mp4>` → `python src/transcribe.py <mp4> work/<id>/transcript.json --lang ja --model small` → 区間選定(llm-response.json手書き or --api) → `node pipeline.mjs render work/<id>`
3. UI/比較は `python -m http.server 8766 --bind 127.0.0.1 --directory ui` → ブラウザ（file://はfetch不可）
4. 機械検証: スモークPASS(11件) / シークレット0件 / コードレビュー指摘=上記D-1〜4,8（実害ゼロ）
