# kosespark Check-out — 2026-06-19

**branch:** master / **HEAD:** （本コミット後に更新）
**次セッションは本ファイル先頭から Read 必須**

---

## 🔴 最重要

1. **video-shorts を「素人配布パッケージ(zip)」化して完成**。成果物 = `products/kosespark/dist/kosespark-video-shorts.zip`（git 管理外・dist/ は .gitignore 済）。マスターがこの zip をそのまま素人に渡せる状態。
2. **P0 ゲートは依然閉**: 即死仮説#1「素人が Claude Code デスクトップを導入・操作できる」は未検証。今回の配布パッケージは、その検証台（素人が自走セットアップできるか）を兼ねる。配布先実機での README 完走実測がゲート検証そのものになる。
3. 配布物はソースを `video-shorts/` から **コピーして自己完結化**したもの。video-shorts/ 本体は無変更。zip を作り直す時は STEP1〜5（plan 参照）を再実行。

---

## A. 本セッション TL;DR

1. マスター要望を確定：video-shorts 機能だけを切り出し、他人の Claude Code に簡単構築できる配布物にする。
2. 設計を「2段構え」で合意：1段目=システム導入（README 駆動で素人の Claude Code が自走）／2段目=毎日運用スキル（チャット発火）。
3. 前提確定：配布先 OS = Windows のみ／選別はチャット完結（ブラウザUI・別サーバー非同梱）／セットアップ時はマスターが画面共有で安全網／LLM区間選定はキーレス（素人の Claude 自身が選定・APIキー不要）。
4. パッケージ作成：`dist/kosespark-video-shorts/` に README.md（構築指示書）・SKILL.md（毎日運用）・はじめにお読みください.txt（素人向け呪文）・pipeline.mjs・src(6)・tests(2) を配置。ui/ は非同梱。
5. zip 生成：`dist/kosespark-video-shorts.zip`（28KB・12ファイル）。
6. 検証：受入基準7件すべて PASS（構成一致・ui非同梱・vibe-base参照0・smoke EXIT 0・zip展開一致・dist gitignore）。

---

## B. 完了 commit 一覧

| hash | 種別 | 内容 |
|------|------|------|
| （本コミット） | chore | kosespark/.gitignore に dist/ 追加（配布パッケージ build 生成物の除外） |

> 配布パッケージ本体（README/SKILL/zip 等）は dist/ 配下で **意図的に git 管理外**。ソースは video-shorts/（既存 git 管理）から再生成可能。

---

## C. 影響波及

- `products/kosespark/.gitignore` に `dist/` 追加。今後 `dist/` 配下は git に乗らない。

---

## D. 残作業

| 優先度 | 内容 |
|--------|------|
| H | P0 ゲート: 配布 zip を実機の素人端末で通し、README 完走（依存導入〜end-to-end〜スキル設置）できるか実測。これが即死仮説#1 の検証を兼ねる。画面共有セットアップ時に実施 |
| M | 配布版の end-to-end 実動画検証（受入#5 相当）は配布先の実動画が要るため未実施。初回 whisper モデルDL込みで通るか確認 |
| L | リポジトリ側 `video-shorts/ui/app.js:23` の job 未サニタイズは未修正（配布物に ui/ 非同梱のため実害なし・別件） |
| L | 次の論点（標準パッケージ構成／ブランドボイス・出典トレース／本格版 SaaS 判断）は未着手のまま |

---

## E. 配備済 subagent / skill / hook

本セッション追加分: なし
（配布物内の `skill/video-shorts/SKILL.md` は素人端末用であり、本リポジトリのスキル登録ではない）

---

## F. 本セッション生成レポート

- 配布パッケージ: `products/kosespark/dist/kosespark-video-shorts/`（README.md / SKILL.md / はじめにお読みください.txt / pipeline.mjs / src / tests）
- 配布 zip: `products/kosespark/dist/kosespark-video-shorts.zip`
- plan: `~/.claude/plans/cozy-giggling-elephant.md`

---

## G. 次セッション初手

1. `products/kosespark/docs/session-reports/2026-06-19-checkout.md`（本ファイル）を Read
2. `products/kosespark/memory.md` P1 を確認
3. マスターに「配布 zip の実機セットアップ（画面共有）を行うか／別論点に進むか」を確認
4. 実機セットアップに進む場合 = P0 ゲート検証を兼ねるため `products/kosespark/hypotheses.md` の #1 条件を Read

**機械検証結果（Wave A-2）:**
- smoke.mjs（配布パッケージ）: PASS（EXIT 0・11 PASS / 0 FAIL）
- npm test / typecheck / lint: SKIP（video-shorts 専用スクリプト未定義）
- secret-grep（配布パッケージ）: PASS（0件）

**コードレビュー指摘（Wave A-3）:** 省略（git 差分は .gitignore 3行のみ・配布物は構造検証済）
