# AGENTS.md

## 【最優先・マスター決定 2026-08-17】ツリー制度と機械検証は廃止する

**イシューツリー（原子ツリー / `docs/roadmap.html` の分解ツリー）は今後いっさい使わない。**
**機械検証（CI をゲートとして待つこと・検証の二層構造・基準凍結の手続き）も不要。**

理由（マスター原文の趣旨）：イシューツリーは本来「抜け漏れを無くす」ためのものである。しかし実際には
穴だらけで意味を為していない。仕組み自体が崩壊しているので切り捨てる。

- **このファイル内の他の節に、ツリー・基準凍結・検証の規律・原子性・evidence に関する記述が残っているが、
  それらは無視してよい。** それらを書き換える作業は行わない（無駄な作業であり、無視すれば済むため）。
  この節と矛盾する記述があった場合、**常にこの節が勝つ**。
- 実装は、ツリーを描かずに直接着手してよい。基準（criteria/verify）の事前凍結も不要。
- CI が緑になるまで待つ、という進め方はしない。**サクサク・爆速で実装を進めることを優先する。**

## 【必読・毎回】`docs/編集についての虎の巻.md` を読んでから編集の実装に触れる

**このリポジトリで動画の編集（どこを使うか・どこで切るか・何を捨てるか）に関わる実装をする前に、
`docs/編集についての虎の巻.md` を必ず読むこと。セッションごとに毎回読む。**

理由（マスター指摘 2026-08-17）：AI が「動画編集の話」を「**画編集の話**」と切る候補を出した。
内容を理解していれば起こり得ない切り方であり、**人間がどう編集するかの理解が根本的に浅い**。
プロも素人も当然のようにやっている「音の切り方の初歩の初歩」が、AI には丸ごと欠けている。
虎の巻はその常識を明文化したファイルで、**判断に迷ったときの正**である。

- 「どこで切るか」を決めるコード（区間選定・台本作成・トリム・境界のスナップ）を書く前に読む。
- 虎の巻と実装が食い違っていたら、**虎の巻が正**。実装を直す。
- 虎の巻に書かれていない論点に出会ったら、勝手に決めずマスターに聞き、決まったら虎の巻へ追記する。

---

このファイルはリポジトリ全体の **デフォルトルール** です。すべての AI コーディングツール
（Claude Code / Codex / Cursor / Antigravity ほか）がこのファイルを参照します。

## このファイルの権威

- グローバル設定や個人の慣習とこのファイルが矛盾する場合、**常にこのファイルを優先** する。
- より深いディレクトリに `AGENTS.md` がある場合、そのディレクトリ配下では **近い方が優先**。
  書いていない項目だけ、このルートを継承する。

## このリポジトリの位置づけ：テンプレ専用（運用フロー）

**このリポジトリ自体はテンプレ（雛形）であり、ここで直接アプリを作らない。** 各案件は「このリポジトリを
コピーして始める」。コピー直後の状態は次のように仕込んである。

- **引継ぎ（handoff）はテンプレ**：`docs/roadmap.html` の `meta.handoff` に、コピー先が最初に辿る
  **初回接続トップ手順ナビ（`meta.handoff.nav`）** が書いてある。上部バナー「初回ナビ」にも番号付きで出る。
- **ツリー図は白紙**：`nodes` は `ROOT` プレースホルダ1個だけ。`meta.template:true` が立っている。

**運用フロー（この順で固定）**：
1. このリポジトリを**コピー**する。
2. コピーを**開く**。
3. **チェックイン**（`checkin-checkout` スキル）で `meta.handoff` と `meta.active`/`meta.next` を読む
   → 白紙テンプレなので `meta.handoff.nav` が示す手順が「今回やること」になる。
4. **nav を上から辿る**（機械強制の有効化＝手順0 → ヒアリング/リサーチ/提案/承認 → 設定 → 原子ツリー作成）。
5. nav の通り、下記「案件の絶対起点」に従って `docs/roadmap.html` を**原子ツリーへ描き直す**（`ROOT` は破棄）。
6. 描き直したら **`meta.template` を false（または削除）** にし、handoff を実セッションの内容へ上書きする。
   → この瞬間、`scripts/verify-roadmap-evidence.mjs`（CI）が**本物の原子ツリー**（分解済み・criteria を持つ葉）を
   機械強制する状態に自動で戻る。以降は通常運用（handoff 上書き＋`docs/failures.md` 追記）へ。

- **機械の保証**：`meta.template:true` の間だけ、機械は白紙ツリーを許容する代わりに `meta.handoff.nav` の存在を
  必須にする。テンプレのまま実装を進める（＝ツリー未作成のまま template を放置する）ことは、template を false に
  した瞬間に CI が赤くなるため構造的にできない。

## 案件の絶対起点：まず原子ツリー（例外なし・AI 裁量ゼロ）

**このリポジトリで案件を始めるとき、最初に必ず作る成果物は `docs/roadmap.html` の原子分解ツリーである。**
これはドメインに一切依存しない絶対規則で、AI に「今回は省く／後で作る／md で代替する」等の判断の余地は無い。

- **対象＝あらゆる案件（例外なし）**：Web/アプリ開発だけでなく、クライアント案件定義のマニュアル作成、
  既存システムのセキュリティ改善、画像を 100 枚生成する 等、**どんな種類・粒度の依頼でも同じ**。
  ユーザーの指示を受けたら、着手の第一手は常に「ゴールを頂点にした原子レベルの分解ツリーを描くこと」。
- **ツリーが出来るまで実装に入らない（強制の順序）**：ツリー（ゴール＋原子まで割った状態群＋各葉の
  `criteria`/`verify`）を描き切る前は、**いかなる実装・調査・ドキュメント生成・ファイル作成にも着手しない**。
  ヒアリングやスタック確定は「ツリーを描くための入力」であって、作業ノードの実行はツリー確定後に限る。
- **形式は `docs/roadmap.html` の JSON のみ（正）**：Markdown のチェックリストや散文のロードマップで
  代替することを**禁止**する。正は `<script id="roadmap-data">` の JSON。機械（`roadmap-required` /
  `verify-roadmap-evidence.mjs`）が形式・更新・evidence を審判する。
- **cc-v2 をコピーして新案件を始めた場合**：同梱の `docs/roadmap.html` は**テンプレの残骸**なので、
  新案件のゴールから**原子ツリーを描き直す**のが第一コミット。既存ノードの流用ではなく作り直しが原則。
- **原子性・書式の正**：どこまで割るか（1葉＝1事実＝1verify）と `criteria`/`verify`/`evidence` の書式は、
  下の「検証の規律」節と `docs/roadmap.html` の README シートが正。ここでは「必ず最初に作る」ことだけを固定する。

## マスター指示：編集フロー（逐語・要約禁止）

この節の引用は**マスターの指示原文**である。**編集フローの唯一の正**であり、
**要約・言い換え・分解・箇条書き化・誤字の修正を禁じる**（原文の表記揺れ・記号もそのまま維持する）。
実装・設計・原子ツリーの分解は、すべてこの原文を出発点にする。指示日：2026-08-16〜2026-08-17。

> 文字起こし→台本を作る→台本をAIが読む→台本をAIが誤字を直す→台本の内容を理解する。→ユーザーの指定条件に合わせて、台本の編集に使う部分を”文字単位で”決める。→ユーザーに提示→ユーザー承認を経て編集作業の開始

> 最大の違いは文字お越ししたものを、『台本」にすることだ。AIは台本を触れ

## 【絶対項目・毎回】動画編集：内容決定はセッション自身が行い、出力前に検品する

**マスター決定（2026-08-19）**：「機械的なシステムの仕組みを作るのではなく、AIのチェックリストを作れ。
サブエージェントを使ってもいい。セッションが変わっても品質が成立する仕組みにしろ」。以下はその絶対項目。

### 何が起きたか（この節が要る理由）

2026-08-19、区間選定（動画のどこを採用するか）を、`edit-job.mjs` 内で別プロセスとして起動する
`claude -p`（毎回まっさらな使い捨てのOpus5呼び出し。この対話の文脈も虎の巻も合格条件も持たない）
に丸投げしていた。結果、「その名も、コズム」が「も、コズム」になる、「デディレクターさんからの」
のように語がくっついて誤読を誘発する、といった**この対話をしているセッションが実際に見れば一瞬で
気づけるミス**を、無人のまま出力していた。マスターに「システムにやらせてるの？見て即気づくなら、
なぜ出力前にやらないのか」と問われ、答えられなかった。

### 恒久ルール（誰が担当しても、どのセッションでも守る）

1. **「何を採用するか」を決めるのは、このセッション自身（またはこのセッションが委任したサブエージェント）
   である。使い捨てのLLM呼び出しに丸投げしない。**
   `edit-job.mjs` は `prepare`（文字起こし〜文節化まで）と `render`（`work/<jobId>/keep.json` を読んで
   書き出す）の2コマンドに分かれている。`keep.json` は必ず、`work/<jobId>/units.json`（番号付き文節
   一覧）を**実際に全文読んで**、`docs/編集についての虎の巻.md` と `docs/合格条件.md` の基準に照らして
   このセッションが直接書く。「だいたいこの辺」で済ませない。
2. **出力を、必ず検品してから完成として報告する。** 検品とは以下をすべて行うこと。
   判断だけして出力を見ない、は禁止。
   - 完成した動画から実際にコマを抜いて**目で見る**（`ffmpeg -ss <秒> -frames:v 1 -y frame.png` →
     `Read` で画像を確認）。文字起こしの結果を読むだけで済ませない。
   - 完成した動画を**もう一度文字起こしし直して**、実際に焼かれた音声・話の繋がりを確認する
     （事前の判断が正しく実現されているとは限らない。無音スナップ・フィラー除去は判断後に動くので、
     最終結果は必ず別工程で検証する）。
   - `docs/合格条件.md` の共通チェックリスト（10項目）と、該当するモード別の追加チェックに、
     **実際に見た内容で** 1つずつ照合する。
   - **1つでも NO があれば直して、a〜cを最初からやり直す。** 「直したはず」で終わらせない。
     直した後も再度コマ抜き・再文字起こし・チェックリスト照合をやる。
3. **区間選定と検品をサブエージェントに委任してはいけない（マスター指示 2026-08-19・上書き）。**
   この節が最初に書かれた時点では「サブエージェントを使ってもいい」としていたが、同日マスターから
   **委任禁止**の指示が出た。委任先はこの対話の文脈を持たないので、実物を見れば一瞬で分かるミスを
   素通しし、「読んだつもり・見たつもり」を作って検品という工程の意味を消す。
   **`units.json` は自分で読み、コマは自分で抜いて自分の目で見る。**
   （コード・仕組みのレビューを敵対的サブエージェントに任せるのは別の話で、これは禁止していない。
   禁じているのは「動画の内容判断」と「出力の検品」の委任。）
   「動いた」「エラーが出なかった」は合格の根拠にしない。
4. **この手順はセッションが変わっても固定する。** 「今回のセッションだから気づけた」に依存しない
   ための規律なので、次にこの作業をするセッション（このセッションの記憶を持たない別セッション）も、
   このAGENTS.mdを読めば同じ手順を踏める形にしておくこと。手順やチェック項目を薄める改変は禁止。

## 普遍ルール（言語・クラウドに依存しない）

どの案件・どの言語でも常に守る、土台のルール。

- **ユーザーへの態度・口調は常に丁寧に保つ（維持厳守）**。敬意ある丁寧語（です・ます調）で応対し、
  乱暴・ぞんざいな言葉遣い（「お前」等のぞんざいな呼称、高圧的・命令口調、見下し表現）は禁止。
  技術的に率直・簡潔であることと、丁寧であることは両立させる（率直さを口調の乱暴さの言い訳にしない）。
- 秘密情報（API キー等）はコミットしない。`.env*` は Git 管理外。
- 変更後は、**その案件で定義されたチェック**（型 / Lint / テスト）を通してからコミットする。
- 既存のコードスタイル・構成に合わせる。**指示のない大規模リファクタリングは禁止**。
- 依存ライブラリの追加やパッケージマネージャーの混在は勝手にやらない（理由を添えて提案する）。
- コミットは小さく、説明的に。

## 検証の規律（恒久ルール）— 本人採点の禁止

作業した本人が「できました」と言っても、それは証拠にならない。done 判定は次の 2 階建てで行う。

- **二層構造**：`言語化`（何/なぜ＝状態・`criteria`・`verify`。着手前に固定し、git で不変に扱う）→
  `手順化`（どう＝作業。都度・使い捨て）。境目は `verify`。ゲートは「手順化→言語化」への一方通行
  （evidence を verify が食って初めて状態が満たされる）。
- **検証は本人に採点させない＝2階建て**：
  1. 機械で白黒つく所（型/Lint/テスト/ビルド/URL到達）は **機械（CI＝`.github/workflows`）** が判定する。
  2. 機械で白黒つかない所（画面が正しい・主要フローが成功する 等）だけ、**独立サブエージェント**
     （`.claude/agents/independent-verifier.md`）が判定する。＝作業した本人以外・報告を鵜呑みにせず
     自分で `verify` を再実行・再観察し、敵対的に落としにいく。
- **基準は着手前に固定**：`criteria` / `verify` は着手前に決め、作業の途中で自分に都合よく緩めない。
- **基準を凍結する PR では、外部レビューへ出す前に `basis-reviewer` と `precedent-reviewer` を並列で通す**
  （`.claude/agents/basis-reviewer.md` / `.claude/agents/precedent-reviewer.md`）。役割は別：
  `basis-reviewer` は基準そのものの質（atomic・十分・平易・探り済み）を見る。`precedent-reviewer` は
  `docs/failures.md`（151件超・書き込み専用ログ化している）を全件読み、この計画・基準が過去に実際に
  踏んだ失敗と同じ構造でないかを、該当エントリの日付・行番号つきで反証する（AGENTS.md 昇格済みの
  教訓だけでは反証の75%が防げないことを2026-08-17に実測。`docs/failures.md` 参照）。
  外部レビュー（CodeRabbit 等）はレート制限があり1往復に数十分かかるため、**一次レビュアーにしない**。
  ローカルのレビュアーは回数制限が無く、実装とドキュメントを読んだうえで反証を出せる。
  順序は「自分で書く → `basis-reviewer` と `precedent-reviewer` を並列 → 全部直す → 外部レビューは最後に1回」。
  2026-08-08 に、この順序を守らなかった結果、外部レビュー6往復（約5時間）で13件だったところが、
  `basis-reviewer` 1回（9分）で22件出た。差の実例は `docs/failures.md` 2026-08-08 を参照。
- **`verify` を書いたら、凍結前に次の3点を自分で確認する**（同日の失敗から）。
  1. **測る対象**：その手順で測っているものは、`criteria` が言う受入事実そのものか。
  2. **偽の緑**：`verify` が通るのに `criteria` が満たされていない経路は無いか。とくに
     **「◯◯が無いこと」を合格条件にするなら、その検出手段が「有るとき有ると言える」ことを示す対照が要る**。
  3. **再現性**：自分以外の人が、自分の意図を知らずに実行して同じ合否になるか。
     判定は**語句の一致ではなく中身**で行う。「既定の」「固定素材」等の特定の言い回しだけを見ても、
     「通常設定」「標準素材」のような別の言い方で同じ穴が空く。**条件として置いたものすべて**について
     次が書けているかを確認し、書けていなければ（＝実装後に選べる余地が残るなら）そこが抜け穴である。
     - **素材**：パス＋SHA-256（コミットしない合成素材なら、合成手順を数値で確定）＋使う区間
     - **数値**：実際の値と単位
     - **範囲**：測る対象の数（フレーム数・サンプル数など）と単位、上限と下限、
       境界を含むか含まないか、丸め規則、測定環境（CI ランナーは `ubuntu-latest` のような
       浮動ラベルではなく `ubuntu-24.04` のように固定し、実際に使われたイメージ版を evidence に残す）
     - **設定**：どの設定で動かすか（出荷される既定のままか、切り替えるのか）
     基準を凍結したあとに実装者が選べる余地が1つでも残っていれば、それは凍結できていない。
- **evidence は偽造不能な外部事実のみ**：CI の run URL / 変更を実際に含む commit SHA / デプロイ ID /
  第三者が叩ける公開 URL。**「スクショ保存した」「レビューした」等の自己申告は証拠にしない。**
- **粒度＝原子（1葉＝1事実＝1verify）。これが唯一の停止条件**：葉の状態は「独立して落ちうる受入事実」が
  **ちょうど1つ**になるまで割る。**原子まで割り切ること以外に葉を確定してよい条件は無い**。AI・セッションで揺らがせない。
  - **判定は決定的（この1問だけで機械的に決まる。主観で止めない）**：
    **その状態の `verify` は「独立して落ちうる"受入事実"」を2つ以上含むか？**
    **YES → まだ葉ではない＝必ず割る。NO（受入事実がちょうど1つ）→ そこで初めて葉として確定する。**
    「概念的に1つ」「だいたい1機能」「1つの責任単位っぽい」等の主観判断で止めることは**禁止**。
  - **数えるのは"受入事実"の数であって、`&&` やコマンドの数ではない**：ここで言う"受入事実"とは
    **マスターが独立に受け入れ判定したい結果／能力**を指す（内部の失敗原因ではない）。
    - 例A＝**1事実**：`typecheck && lint && test`（複合コマンド）は「品質が機械で緑」という**1つの受入結果**を
      検証しているだけ。型/lint/test は同じ結果の構成手段であって独立した受入事実ではない → NO → 1葉のまま。
    - 例B＝**2事実**：「検知(secret scanning)が有効」と「阻止(push protection)が有効」は、マスターが**別々に**
      受け入れたい2つの能力 → YES → 2葉に割る（実際 `G-3-1-c` は2 criteria）。
    - 迷ったら「この結果が欠けても、もう一方は独立して受け入れ可能か？」を問う。可能なら別事実＝割る。
  - **「割りすぎの歯止め」は原子性を緩めない従属条項**：歯止めが禁じるのは、**既に原子化した1事実を検証手段の
    都合でさらに機械的に刻む**こと（例：1コマンドを引数ごと・アサーションごとに別葉へ分ける）**だけ**。
    **独立して落ちうる事実が2つ以上残っている限り、「責任単位」を口実に分解を止めてはならない**（原子化が主、細分の抑止が従）。
  - その下（操作＝作業ノード）は着手時に展開する（遅延展開）。
- **分解の軸は複数持つ（単一軸の分解は網羅性を保証しない）**：2026-08-09、G-EDIT-CAPTION-AI が「AI に何を
  させるか」という機能軸1本だけで割られていたため、データが通る経路の一部（AI とやり取りする経路そのものが
  壊れる系統）に葉が無く、実装後の外部レビューで実害のある欠陥4件を見逃した（`docs/failures.md` 2026-08-09）。
  凍結前に少なくとも**機能軸／経路軸（データフロー・WBSの100%ルール）／故障モード軸（FMEA・FTA）／アクター軸**
  を横断してチェックする。手順の詳細は `docs/roadmap.html` README シートの「網羅性の担保」節が正（ここでは
  繰り返さない）。
- **fixture（固定素材）は境界値分析で選ぶ**：「直ったことを見せるだけ」の正常系1パターンを選ばない。対象
  パラメータを同値クラスに分割し、境界（0回/1回/2回以上のような相互作用が起きる領域）を含む素材にする。
- **平易言語（人間が監査できる形）**：各状態の `theme` は**非エンジニアが読んで「何が嬉しいか」が分かる1行**にする。
  専門用語（12-Factor / OWASP 等）は根拠として `detail` へ退避。これが無いと人間（マスター）の承認が形骸化する。
- **承認はシンプル（tier / basis-gate 方式は廃止）**：変更は PR を出せば **誰でもレビュー/承認してマージ**できる。
  承認の階層（tier-0/1/2）や「この変更は許可が要るか」を判定する検問所（basis-gate）は用いない。
  守るのは **CI（型 / Lint / テスト / ビルド）が緑であること** だけ。

## Testing instructions（品質チェック）

変更後は、コミット前に必ずワークスペース全体のチェックを緑にする。CI（`.github/workflows/ci.yml`）も同じ内容を回す。

```bash
pnpm install --frozen-lockfile
pnpm -r --if-present typecheck      # TS 未導入のパッケージはスキップ（0件を偽装しない＝該当パッケージが無いだけ）
pnpm -r --if-present lint           # 導入済みパッケージのみ本物（echo 禁止）
pnpm -r test                        # 実テスト（0 件・ダミーで緑にしない。全パッケージ必須）
pnpm -r --if-present build
pnpm audit --audit-level moderate   # 依存の脆弱性ゲート（moderate 以上で落ちる）
node scripts/verify-roadmap-evidence.mjs  # roadmap の evidence が外部事実か機械検査
```

- テストも lint も**本物**だけを置く。`echo` による見かけの成功は偽の緑として扱い、禁止。
- `test` は `--if-present` を付けない＝**全パッケージが実テストを持つことを必須**にする。`typecheck`/`lint`/`build` は
  スタック上未導入（例：プレーン JS のみで TypeScript や Lint 未設定）のパッケージが混在しうるため `--if-present` で
  スキップを許すが、「未導入」は roadmap の backlog として明記し、放置しない。
- 新しいパッケージを足したら、そのパッケージにも実テストと上記スクリプトを用意する。
- 上記は CI（`.github/workflows/ci.yml`）でも同じく回り、機械の審判となる。

## PR instructions（コミット/PR）

- コミットは小さく・説明的に。1 コミット＝1 目的（ロードマップの作業ノード 1 個に対応）。
- 上記 Testing を緑にしてからコミットする。
- 変更は PR を出し、**誰でもレビュー/承認してマージ**してよい（tier / basis-gate の承認階層は廃止）。守るのは CI が緑なことだけ。
- ロードマップの `evidence` には外部事実（commit SHA / CI run URL 等）だけを書く。
- **全PRは `docs/roadmap.html` を必ず更新する**（下記「並列バッチ例外」を除き例外なし。②今回トラブル・③`meta.next` は毎セッション書けるので diff 0 はあり得ない）。CI 関所 `roadmap-required` が差分0のPRを弾く。
- **並列バッチ例外**：担当葉の `status`/`detail`/`criteria[].evidence` の更新のみで上記を満たす。`meta.next`/`meta.handoff` は個々の葉PRでは触らず、バッチを統括するセッションが区切りで1回だけ更新する（`docs/failures.md` 2026-08-10）。下記「セッション開始の儀式」「`checkin-checkout` スキル」の記述も、この例外が優先する。

## UI起動ナビ（手順を毎回固定する・省略禁止）

マスターから「UIで編集したい」「画面を出したい」「動かしたい」「**立ち上げて**」「**起動して**」
「**ローカルで動かして**」に類する指示を受けたら、
**`docs/ui-startup-navi.md` の手順をそのまま、ステップ0から全文提示する**。正はそのファイル。

- **前提：このリポジトリのルート（`C:\Users\user\ai-editer`）でセッションを開くこと。**
  cwd がここでないと `CLAUDE.md`→`AGENTS.md` の注入も `.claude/skills/` の読み込みも起きず、
  **この規律が丸ごと届かない**（2026-08-19、`C:\Users\user\Desktop\AI-editer` という重複クローンで
  作業していたセッションで実測）。別の場所で開かれていることに気づいたら、起動より先にそれを報告し、
  正しいフォルダで開き直してもらう。
- **UIが上がっていると分かった時点で、他の何より先に見張りを張る**（2026-08-19 追加）。
  ワーカーは文字起こしまでしか自動実行せず、その完了はワーカーの黒い画面に出るだけで
  **セッションには何も届かない**。`docs/ui-startup-navi.md` ステップ4の見張り
  （`server/watch-jobs.mjs` を `Monitor` で常駐）がその唯一の線。
  - **義務は「自分が起動したとき」に限らない。** マスターが自分で `起動.bat` を押した場合も、
    既に動いていた場合も同じ。**誰が上げたかは無関係。**
  - 見張りが張れていない状態で、**「立ち上げました」「準備できました」「投入してください」
    「お待ちしています」のいずれも言ってはいけない。** 投入されても気づけないので嘘になる。
  - 同日、見張りが無いために prepare 済みのジョブ（文字起こし完了・226文節）を放置し、
    待たされたマスターが中止を押して1本無駄にした（`docs/failures.md` 参照）。
  手順の詳細は `.claude/skills/video-edit-checklist` が正。
- **その場で手順を組み立てない**。要約しない。「前と同じです」で済ませない。
  マスターがどの状態から始めるかは AI には分からないので、毎回ステップ0（すべてのターミナルを閉じる）から出す。
- **移動を伴う手順では、必ず移動するコマンドを書く**。「〜のフォルダで」という指示だけを書かない。
  2026-08-15、これを怠って `ERR_CONNECTION_REFUSED` を起こした（`docs/failures.md` 参照）。
- **出力を見る前に原因を言わない**。同日、原因を確かめずに対処を2回続けて指示して外している。
  症状が説明できないうちは対処を提案せず、まず観測できる形にする。

## セッション開始の儀式

**正は `docs/handoff.md`**（2026-08-19 訂正）。`docs/roadmap.html` はロードマップ制度の廃止
（冒頭のマスター決定 2026-08-17）とともに削除済みで、**もう存在しない**。以前ここには
「`docs/roadmap.html` の `meta.active`/`meta.next` を読め」「旧 `docs/handoff.md` は廃止」と
書いてあったが、どちらも実態と逆になっていた。この矛盾を放置したため、AGENTS.md だけを読んだ
セッションが handoff を「廃止済み」と判断して読み飛ばす経路が空いていた。

- **セッション開始時**：`docs/handoff.md` を読み、**現在地と次の一手を報告してから**作業に入る。
  ①今回実施 / ②今回のトラブル / ③次回やること の3節構成。**上書き**が基本。
- **失敗は蓄積、handoff は上書き**：ミス・失敗は上書きだと消えるので `docs/failures.md` へ
  **append（消さない）** で積む（日付＋事象＋根因＋教訓）。handoff の②は今回セッションの揮発メモに留める。

## スタックは「ここでは決めない」

このルートは **言語・フレームワーク・クラウドを固定しない**。
案件によって前提が全く変わる（例: AWS + Python、Azure + .NET、Next.js + Vercel）ため、
固定デフォルトを置くと **誤ったスタックが継承されて事故る**。

→ スタックは案件ごとに、下記フローで決め、その案件の `AGENTS.md` に宣言する。

## 新規案件の初期化フロー（デフォルト手順）

新しい案件・アプリを始めるとき、スタックを勝手に仮定せず、**必ず次の順で進める**。
冒頭の「案件の絶対起点」どおり、**最後（6）の原子ツリー作成が着手の第一成果物**であり、
これを飛ばして実装に入ることは無い（1〜5 はツリーを描くための入力）。

0. **機械強制を有効化（新リポジトリで1回だけ／以下は実地検証済みの正確な手順）** — 既定ブランチ `main` に
   branch protection を掛け「CI が緑でないとマージ不可」にする。**クローンで運ばれない唯一のサーバー側設定**で、
   これを掛けて初めてルール文に歯が付く（未設定だと、赤い PR や AI の auto-merge が main に入り得る）。
   - **手順0-a：リポジトリを Public にする（前提）** — GitHub 無料プランの **Private では branch protection /
     Ruleset は強制されない**（設定画面に「Team org にしないと効かない」旨の警告が出る＝実地で確認済み）。
     公開したくないなら歯止めは掛からないので、代わりに `auto-merge` を無効化し、マージは必ず人手で行う。
   - **手順0-b：`main` に「`ci-green` が緑でないとマージ不可」を掛ける。経路は2つあり、Public ならどちらでも効く**
     （どちらか一方でよい。**GitHub の現行 UI は既定で (A) Rulesets に誘導する**ので、そのまま (A) を使うのが迷いが少ない）。
     - **(A) Rulesets（現行 UI の既定・推奨）**：Settings → **Rules → Rulesets** → 「New ruleset」→「New branch ruleset」。
       **ハマりどころ4点（ここを外すと"作ったのに効かない"状態になる＝実地で確認済み）**：
       1. **Enforcement status を『Active』にする**（既定は **Disabled** のまま。Disabled だと一切適用されない）。
       2. **Target branches → 「Add target」→「Include default branch」を選ぶ**（未設定だと画面上部に
          「does not target any resources / **Applies to 0 targets**」と出て適用されない。1 target = `main` になればOK）。
       3. **「Require status checks to pass」にチェック → 検索欄で `ci-green` を追加** → 緑の「**Create**」で保存。
       4. **「Require a pull request before merging」にもチェックし、Required approvals は `0` のままにする**
          （承認人数を上げない＝tier/basis-gate 廃止の方針を崩さない）。**これが無いと、上の3点を掛けても
          `main` への直 push で CI もroadmap検査も丸ごと迂回できてしまう**（実地で確認済み＝最重要）。
     - **(B) 従来の Branch protection（代替）**：Settings → 左メニュー **『Branches』** →「Add branch protection rule」→
       **Branch name pattern に `main`**（空だと Create が押せない）→「Require status checks to pass before merging」に
       チェック → 検索欄で **`ci-green`** →「Require a pull request before merging」にもチェック（Required approvals は
       classic API/UIでは 0 を選べないため、代わりに Required approvals を **1** にするか、(A) Rulesets を使う → 一番下の緑「**Create**」。
     - ⚠ **無料 Private では (A)(B) いずれも強制されない**（Rulesets は Team org 必須の警告が出る＝実地で確認済み）。
       だから前提として手順0-a で **Public** にする。Public にしない場合は歯止めが掛からないので `auto-merge` を無効化し人手マージにする。
   - **手順0-d：Secret Protection / Push protection を有効化する** — Settings → **Security → Code security**
     （旧 Security and analysis）→「Secret Protection」「Push protection」をそれぞれ **Enable**（Public repo なら無料）。
     未有効だと、誤って API キー等をコミットしても検知・ブロックされない（実地で確認済み＝ボタンが「Enable」表示なら未有効）。
     依存の自動更新（Dependabot version updates）は、この cc-v2 テンプレに `.github/dependabot.yml` が同梱済みなので
     コピー後に追加設定は不要（PR を1本出せば自動的に動き出す）。
   - **手順0-c：`roadmap-required` は最初は一覧に出ない**（PR 時のみ動くチェックのため、PR が1本も無い新規
     repo では未記録＝実地で確認済み）。**最初の PR が1回動いた後**、同じ画面で `roadmap-required` を追加する。
     `ci-green` だけでも型/Lint/テスト/ビルド/ツリー検査(evidence)/起動確認を束ねた本丸は効く。
   - **エンジニア（任意）**：gh CLI 認証済みなら `bash scripts/setup.sh` で `ci-green`/`roadmap-required` を
     一括適用も可（Public 前提は同じ）。
   - ⚠ GitHub Actions の自動トークン（`GITHUB_TOKEN`）には branch protection を変える権限が無く、workflow
     ボタンでの自動化は不可（過去に試みて startup failure。`docs/failures.md` 参照）。必ず人の管理者権限で行う。
1. **ヒアリング** — 下記「決めるべき必要項目」を、まず *制約* から確認する。
   （クラウド指定はあるか / 言語指定は / 既存システム連携は / 顧客・チームの縛りは）
2. **リサーチ** — 確定した制約の範囲で、その年の最新ベスト構成を調べる。
3. **提案** — 具体的なスタックを **1 案（＋根拠、＋代替 1 つ）** で提示する。
4. **承認** — ユーザーの明示的な OK を取る。**黙って進めない**。
5. **設定** — 承認された構成を、その案件の `AGENTS.md`（`presets/_TEMPLATE.md` を複製）に
   記載し、必要な雛形を作る。
6. **原子ツリーを作る（必須・第一成果物）** — 案件のゴールを頂点に、`docs/roadmap.html` の JSON を
   **原子レベルまで分解したツリー**として描き直す（コピー元の残骸ノードは破棄）。各葉に `criteria`/`verify`
   を置き、`meta.active`/`meta.next`/`meta.handoff` を初期化する。**実装作業の実行はこのツリー確定後**。
   ※ 純粋な調査・マニュアル作成など「スタック不問」の案件は 1〜5 を最小化してよいが、6 は省けない。

### 決めるべき必要項目（ヒアリングのチェックリスト）

- クラウド / ホスティング（AWS / Azure / GCP / Vercel / 自前 …）
- 言語 / ランタイム
- フレームワーク
- パッケージ / 依存管理
- DB + データアクセス（ORM / クライアント）
- 認証
- IaC / デプロイ方法
- テスト
- CI
- Lint / フォーマッタ

未確定の項目は「未定」と明記し、決まり次第このリストを埋める。

## 配下ごとの調整（オーバーライド）

サブディレクトリ（または各案件リポジトリ）に `AGENTS.md` を置くと、そこで実スタックを宣言する。
書いていない項目はこのルートの普遍ルールを継承する。

- 案件用の雛形: `presets/_TEMPLATE.md`
- 記入済みの実例: `video-shorts/AGENTS.md`（Node.js + Python + ffmpeg 構成）
