# kosespark failures.md

> 1 失敗 = 1 行。具体策のみ。経緯禁止。200 行上限。
> **Check-out 時に本セッションの失敗を必ず追記すること**（2026-06-11 横断調査で 19 製品中ほぼ全てが空 = 形骸化が確認された。書かない Check-out は不完全）。

| 日付 | 失敗 → 再発防止策（1 行） |
|---|---|
| 2026-06-16 | package.json に `"type":"module"` を入れ CommonJS hook(post-bash.js) を破壊 → 製品ルートの package.json `type` は既存 .js hook への影響を確認。不要なら入れない |
| 2026-06-16 | コンセプト/事業モデルを反復誤認（Claude完結化・"再販"と取り違え）しマスター差し戻し多数 → 着手前に CLAUDE.md 冒頭の根幹を毎回 Read し、解釈を1文で確認してから進む |
| 2026-06-23 | claude -p 区間選定が 300s タイムアウト。「入力サイズが律速」と誤認しチャンク 20→10 分に縮小→無効 → 真因は毎回の MCP(serena 150s+) ブート。`--strict-mcp-config` で MCP ゼロ化し 300s→5.5s。詳細 docs/session-reports/2026-06-23-claude-p-timeout-fix.md |
| 2026-06-23 | claude -p 軽量化で `--bare` を採用しかけたが OAuth ログインを剥がし `Not logged in`→無料運用破壊 → MCP だけ切る `--strict-mcp-config` を使う。`--bare` はサブスク運用で使用禁止 |
| 2026-06-23 | 地の文に擬似 invoke タグを書きツール呼び出し解析失敗を3回 → ツール呼び出しは必ず正規機構で発行（feedback_tool-invocation-format.md） |
| 2026-07-02 | Groq コンソールのキー発行ナビを実画面未確認で推測し3回失敗（Submit 等が実 UI と不一致）→ 外部サービスの UI 操作ナビは推測禁止。WebFetch で公式手順を先に確認するか実画面スクショ取得後に案内する |
| 2026-07-02 | 同一判定（鍵の有無）を Node/Python で二重実装し優先順位が逆転（env→.env vs .env→env）。並列制御と実働バックエンドが食い違うバグに → 同一判定の多言語実装は片方を正と明記し優先順位・空値ケースの一致テストを書く（checkout検証パネルが検出・即日根治 9e795722e） |
| 2026-07-03 | video-shorts Webアプリ化の技術詳細を1人で深掘りし平易化前に承認要求を重ね、Plan Mode再入・複数回差し戻し発生（マスター「お前だけ理解してる状況で先に進むな」） → 深い調査後は時系列の平易な説明＋理解確認の質問を挟んでから提案する（feedback_dont-outpace-user-understanding.md） |
| 2026-07-04 | `CLIP_PAD_HEAD`等のenv数値変換で`Number()`のみ・`Math.max(0,NaN)`もNaNのままでNaN伝播を検知できず → env由来の数値は必ず`Number.isFinite`で明示ガードし不正値は既定値へフォールバックする統一パターンを使う |
| 2026-07-04 | P0即死仮説#1「素人が導入・操作できるか」の検証を何度も蒸し返しマスターを不愉快にさせた（「解決で終われ、永遠に提示するな」） → 即死仮説#1は解決検証完了（hypotheses.md #1=✅）。**この検証・P0ゲート未通過を理由にした進行停止を二度と提案しない**。マスターが「解決」と断じた論点は再提示禁止 |
| 2026-07-05 | 委任先エージェントに進捗確認をSendMessageで尋ね続け、実ファイル（timing.json/ffprobe/git diff等）を自分で見に行くのが遅れマスターを不愉快にさせた（「もうお前やって来い」） → 進捗確認を求められたら委任先に聞く前にまず自分でBash/Read/ffprobe等で実体を直接検証する |
| 2026-07-23 | 07-05 に別セッション（worktree `claude/agitated-wescoff-4e45ec`）へ投げた字幕バグ修正が**未コミットのまま18日放置**され、memory.md P1 も07-05で停止したまま07-12の3コミットを記録できていなかった → 委任先を worktree/別セッションに出したら、**次セッションの Check-in で `git worktree list` と各 worktree の `git status` を必ず見る**。委任は投げた時点でなく回収した時点で完了 |
| 2026-07-23 | 字幕ずれの起票時「原因は setpts と ass の順序」と推定されており、その推定パッチを検証前に信じかけた。実測すると出力シークが filtergraph 後段で効くのが真因で、**推定パッチは適用しても何も変わらなかった**（修正前後で同一フレーム） → バグ修正は「①実素材で再現 → ②計測で真因を特定（showinfo等）→ ③修正 → ④同じ計測で再確認」の順を崩さない。起票時の推定原因は仮説であって根拠ではない |
| 2026-07-23 | Bash ツールで PowerShell の here-string（`@'...'@`）を使いコミットメッセージ先頭に `@` が混入（b3b61e1）。`git commit --amend` も `git reset` も Deny のため修正不能で履歴に残った → 多行文字列を渡すコミットは **PowerShell ツール**で `@'...'@`、Bash ツールなら本物の heredoc を使う。ツールとシェル記法の対応を取り違えない |
