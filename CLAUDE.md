# ⚠️ 絶対厳守 — kosespark の根幹（変更厳禁・最優先）

**kosespark は「Claude 以外の外部AIツールと連携する」プロダクトである。**

- Claude Code 単体（Claude 本体 / subagents）で完結させることは**厳禁**。完結はコンセプトの根っこを変える行為であり、製品そのものの否定にあたる。
- 専門作業（画像・音声・動画・検索 等）は**外部の別AIツール**に行わせる。Claude Code はそれらを束ねるハブ／窓口に徹する（＝Genspark の簡易版）。
- **束ねる対象は「専門特化のAI製品そのもの」**（例: Jasper / SAKUBUN / Surfer 等。一見"競合"に見える製品も含む）。**それらを統合し、素人が"1つの窓口"で全部使えるようにすることが価値**（＝Genspark型アグリゲーター・AI統合ツール）。「競合だから組み込まない」は誤り。束ねる側が kosespark。
- subagents は中身が全て Claude のため「連携」とみなさない。代替禁止。
- 「無料で連携」＝鍵不要 or 無料枠のある外部AIツールに MCP / API で接続すること。
- **事業モデル＝「客が自分のアカウント・自分の鍵で契約し、kosespark が構築・設定・保守を代行する」**（§0「API登録代行・保守」）。kosespark 自身のアカウントを客に使わせる"再販/プロキシ"ではない。
  - ＝**PC セットアップ代行・IT 構成代行と同じ正規の受託**。各ツールの「再販・サブライセンス禁止」条項には**当たらない**（再販でないため）。**「束ねたら規約違反では」と再検討するな**（このモデルでは違反しない）。
  - 唯一の個別確認点：**API が無く UI 自動操作（bot）するツール**のみ "自動操作禁止" 条項を確認する。API があるツールはこの問題なし。

> この原則に反する設計・実装・提案は着手前に停止し、マスターに確認すること。

---

@../../.claude/rules/permission-defense.md
@../../.claude/rules/session-lifecycle.md
@../../.claude/rules/plan-mode.md
@../../.claude/rules/self-verification.md
@../../.claude/rules/orchestration-discipline.md

# kosespark 自治憲法 v0.1

@../../docs/ops/agent-rules.md

## §0. プロダクト定義

| 項目 | 内容 |
|---|---|
| 名称 | kosespark（KoseSpark） |
| コンセプト | ジェンスパーク的な"AI統合ツール"をカスタムメイドで構築販売・API登録代行・保守。AI専門家の多様化で置いてきぼりになる素人向けに、チャットだけで複数の専門特化AIを同一プラットフォームで使えるようにする。AI顧問としての武器 |
| 技術スタック | **Claude Code 拡張型（5型外・新規）** = skills / MCP / subagents / hooks のパッケージ。手始めは Claude Code デスクトップ版をプラットフォーム代わりにし、Claude Code と連携する |
| デプロイ先 | Claude Code デスクトップ版（手始め・プラットフォーム代わり）。将来の自前プラットフォーム化は P 周回で判断（マスター追加情報待ち） |
| 現在フェーズ | P0 |

## §1. 最重要ルール

- マスター追加情報待ち（「勝手に変えるな」系の禁止事項はマスター確認のうえ記載。独断記述禁止）

## §2. 技術ルール

- マスター追加情報待ち（Claude Code 連携の具体仕様＝どの skill/MCP/subagent を顧客ごとにパッケージするか・API 登録代行の鍵管理方針は未確定）。サイレントフェイル禁止
- **1ファイル原則500行以内（line-count-gate hook で強制）。** 機能追加は新規ファイル優先とし既存からは import 参照のみ。入れ子は3段以内。単一HTML型でも CSS / JS は別ファイルに分離する（分割手順は code-splitting スキル）

## §3. デザイン仕様

- マスター追加情報待ち（顧客向け UI/導線が発生する場合は web-design スキル「金太郎飴脱却」9 項目を適用）

## §4. 運用メモ（司令塔からの戻し）

- [2026-06-16→2026-07-04] 即死仮説#1＝「非エンジニアの素人が Claude Code デスクトップ版を導入・操作できる」は **解決検証完了（マスター確定 2026-07-04・hypotheses.md #1=✅）**。⛔ **この検証を二度と提案するな**（何度も蒸し返しマスターを不愉快にさせた）。ブロッカー解除。
- [2026-06-16] 技術型は既存5型（HTML/React/Svelte/Workers/GAS）に当てはまらず「Claude Code 拡張型」として新設。将来 SaaS 化する場合に備え層分離を意識する。
- [2026-06-20] **video-shorts 仕様（マスター確定・全セッション適用）**: (1) ショートのレンダ前に**字幕の有無を毎回ユーザーに質問する**（デフォルト決め打ち・前回回答の引き継ぎ禁止。字幕なし=`node pipeline.mjs render <work> --no-sub`）。(2) 区間選定では**明らかなNG・準備中（機材確認／音声テスト／カット相談等の本編でないやり取り）は捨てる**。正本=配布版 `dist/.../skill/video-shorts/SKILL.md` 手順5.5＋手順5。
- [2026-07-02] **video-shorts 素材導入時ヒアリング（マスター確定・全セッション適用・機械強制）**: 素材を受け取ったら**編集モードと字幕有無を毎回ヒアリングする**（前回回答の引き継ぎ禁止・決め打ち禁止）。編集モードは**2択** — (a) `topic`=話題毎（話題の切れ目ごとに1本・分数不問・全編カバー・時系列） / (b) `digest`=ダイジェスト（面白い所だけを1本に。**AI編集エージェント**が 全体理解→台本再構成（並べ替え可・声は素材のまま逐語）→検証修正ループ→完成）。AI 自己規律に頼らず `pipeline.mjs init` が `--mode` と `--sub` を**必須引数**として強制する（未指定はエラーで停止＝訊かずに進めない）。回答は state.json に保存され select/render が参照する。※旧「ショート」モードは話題毎と役割重複のため廃止（2026-07-02）。
- [2026-07-03] **video-shorts 余韻パディングは常時オン（マスター確定・全セッション適用）**: クリップ前後に余韻（リード/テール）を必ず入れる。語境界ピッタリ切りは「ぶち切れ」感が出て編集として常に不自然＝例外なく標準適用。既定=前 0.5s / 後 0.8s（テールを長めに＝話し終わりの余韻が切れると不自然なため）。`pipeline.mjs render` が全レンダに自動適用（オプトインではない）。数値のみ env `CLIP_PAD_HEAD` / `CLIP_PAD_TAIL` で調整可。素材端でクランプ。配布版（`dist/.../pipeline.mjs`）へも反映すること。
- [2026-07-03] **video-shorts 導入時ヒアリングに「縦/横」を追加（マスター確定・全セッション適用・機械強制）**: 素材導入時は**編集モード・字幕有無に加え、出力の縦横を毎回ヒアリングする**（前回引き継ぎ禁止・決め打ち禁止）。**縦=portrait（1080×1920・SNSリール等の縦枠向け）/ 横=landscape（1920×1080・画面録画など細かい文字を残す用途向け）**。`pipeline.mjs init` が `--orient <縦|横>` を**必須引数**として強制（未指定はエラーで停止）。回答は state.json（`orient`）に保存され render が参照。判断軸＝「縦型化は横長・細かい文字の画面録画には不向き（実効解像度低下で文字が潰れる＝§4残課題）→そういう素材は横で出す」。配布版（`dist/.../pipeline.mjs`）へも同変更を反映すること。
- [2026-06-20] **プロダクト方向（マスター確定・要 plan 化）**: video-shorts を「ターミナル操作」から「**客のPC上で動くローカル動画編集Webアプリ**（字幕ON/OFF・エフェクト種類＋時間・カット追加・文字起こし表示）」へ進化させる。エンジン（whisper/ffmpeg/区間選定）は同一PC内で動きクラウド不要＝運営コスト≒0。裏で Claude が編集判断。**まずマスター自PCで実用検証 → 後で客へ配布**（現「配布パッケージ」路線の Web UI 版）。三角制約=[重い動画処理]×[ホスト型クラウド]×[コスト0]は2つまで→計算を客PCに寄せて解決。注意: 客PCローカルアプリのUIは localhost 配信が自然、独自ドメイン配信→localhost は mixed-content で詰まるためブランドはUI内で表現。**着手は P0 ゲート（即死仮説#1）通過後**。今は方向確定のみ。最終ゴール=動画→Instagram(Reels)/X/ブログ自動投稿まで（X API無料枠とストレージがコスト穴・ギガファイル便はDB/恒久保存に不可）。
- [2026-06-20] **字幕/エフェクト技術方針（要 plan 化・P0ゲート後）**: 段階的に — (1) まず **ASS/libass**（ffmpeg同梱・無料）で字幕の凝りを使い切る。(2) TikTok風の動く字幕・モーショングラフィックが要れば **Remotion**（将来のWebアプリReactと地続き・ローカルrender＝コスト0・`remotion-best-practices`スキルあり）。弱点=Chromium経由で重い・チーム規模拡大で有償ライセンス。(3) 全部Remotion化せず **ffmpegで切出し＋Remotionでオーバーレイ層だけ合成**のハイブリッドが定石。(4) コンセプト分岐: Remotion=自作路線（§0「束ねる」からやや外れる）／**外部字幕AI（Submagic/Captions/Opus等）を束ねる=コンセプト一致だが有料・API有無要確認**。判断軸＝「凝った字幕は"外注すべき専門AI仕事"か"自作レンダリング"か」。
- [2026-06-20] **ローカルWebチャット手法 確立（調査済・正本 `docs/method-local-web-chat-bridge.md`）**: 客PC上の「Web編集UI＋チャット欄→ローカルNode橋→Claude→SSEストリーム」を組む確立手法。採用=**`claude -p`（CLI）を subprocess で spawn**＝既存サブスクログイン継承で**コスト0**（Agent SDK は既定APIキー＝従量課金なので構造参考のみ）。手本=**sugyan/claude-code-webui（MIT）**。⚠ claudecodeui/opcode は **AGPL-3.0** で商用クローズド製品の土台にfork不可。次の一歩=実機で `claude -p "..." --output-format stream-json` 検証→`python http.server`を小Nodeサーバ化し`/chat`(SSE)追加。
- [2026-06-20] **【残課題】編集後の画質劣化（要 plan 化・優先度中）**: 縦型ショート出力で画質が落ちる。実測した主因2点 — (1) **実効解像度低下が主因**: `render-vertical.mjs` は上下帯方式（元映像を画面幅1080に decrease 縮小し中央配置・上下は背景ぼかし帯）。16:9 1080p 素材だと中央の映像は約1080×607相当に縮み、見かけの精細感が下がる。これは無理な拡大劣化を避けた意図的トレードオフ。(2) **再エンコード**: `libx264 -crf 20 -preset veryfast`（背景帯合成＋字幕焼きで映像は必ず再エンコード）。音声は `-c:a copy` で無劣化。改善候補=①`-crf 18`＋`-preset slow`でエンコード劣化最小化（速度↓・サイズ↑）②全画面クロップモード追加で帯を無くす選択肢（中央以外が切れるが精細感最大）。Web UI の「投稿先プリセット」と連動させ画質モードを選べるようにするのが筋。
- [2026-06-20] **【残課題】文字起こし高速化（要判断）**: ローカル whisper は CPU 律速（この端末 GPU 非搭載）で遅いが無料。高速化はクラウド文字起こしAPI（有料・分課金・§0「束ねる」と整合）か GPU ハード代の二択。サブエージェント/ソフト工夫では速くならない。無料維持か速度優先かは未判断。

## §6. 禁止事項

- `main` への `--force` push / シークレットのコミット / `archive/` 無断削除
- シークレットの貼付禁止（プロンプト・チャット履歴・エラーログ・スクショ含む）
- **API 登録代行で預かる顧客の API キーを git 追跡・平文保存・チャット貼付しない**（鍵管理方針は §2 で確定するまで実装着手禁止）
- Runbook 整備義務（`docs/standards/templates/runbook-template.md` 参照）

---
v0.1 | 起草 2026-06-16
